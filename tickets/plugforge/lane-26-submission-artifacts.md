# L26 · Docs, Cost Analysis & Submission

| | |
|---|---|
| **Agent** | `submission-artifacts` |
| **Tier** | 9 — last; blocks on every other lane |
| **Block** | PF-781–820 (35 allocated, PF-816–820 reserved for audit) |
| **Blocks on** | All lanes. Mechanically: L01 (PR discipline + baseline), L13 (spec), L21 (deployment), L25 (Pre-Search) |
| **Unblocks** | — |
| **MVP gate** | Item 9 — *"Existing Playwright regression suite passes on main; P95 latency, bundle size, and per-route query counts within +10% of the Part 1 baseline"* (p.2). Everything else here is graded at Final. |
| **Output** | `SUBMISSION-PLUGFORGE.md`, `docs/ai-cost-analysis-plugforge.md` (**not** `docs/ai-cost-analysis.md`, which is Week 5's and cites the ShipShape brief — see PF-794), `docs/per-epic-writeup.md`, `docs/three-discoveries.md`, reconciliation edits to `docs/architecture.md`, and a CI regression-budget job |

**What this lane is.** The Submission Requirements table (p.12–p.13) is ten rows. Nine other
lanes produce the artifacts those rows point at; this lane is what turns "the artifact exists"
into "the deliverable is submitted." Its acceptance criteria are therefore mostly *checks* —
the observable condition is what makes an artifact complete, not what makes it written.

**Where the Submission Requirements table actually lives.** The heading, the deadline line, and
the first two rows (GitHub Repository, Demo Video) are on **p.12**; the remaining eight rows
(Pre-Search through Social Post) are on **p.13**. The Required Documentation nine-section
Section/Content table is also **p.12**, under a heading whose last line sits on p.11. Every
citation below was grep-checked against `.claude/prd/page-N.txt`; L25 cites p.13 for
Submission Requirements and is right about its own row and wrong about the heading — both
halves are true and neither should be "corrected" at audit.

**Three rows this lane does not author.** Pre-Search (L25), OpenAPI generation (L13), and
deployment (L21). PF-812, PF-813 and PF-814 are submission-readiness checks over their output,
deliberately not re-statements of their work. If a check here starts describing *how* to build
the artifact, it has drifted into another lane.

**`docs/architecture.md` already exists and already covers all nine required sections.** So the
architecture-document work is verification and reconciliation. `lane-99-unassigned.md`
§ *Documentation drift* logs four places (G1–G4) where the document contradicts the code; each
gets its own reconciliation ticket, and G3's fix already belongs to L21 PF-647 — PF-790 verifies
that landing rather than redoing it.

## Tickets

| ID | Title | Acceptance criterion | Advances | PRD | Deps |
|---|---|---|---|---|---|
| PF-781 | ☐ `SUBMISSION-PLUGFORGE.md` — one row per Submission Requirements deliverable | Exactly **ten** rows, named verbatim from the PRD table (GitHub Repository, Demo Video, Pre-Search Document, Architecture Document, OpenAPI Spec, AI Cost Analysis, Per-Epic Write-up, Three Discoveries, Deployed Application, Social Post); each row carries the owning lane, the artifact path or URL, and a Ready / Not-ready state. A grader checks completeness by counting rows, and a row with no resolvable path is Not-ready by definition | — | p.12, p.13 | — |
| PF-782 | ☐ Resolve the Final Submission deadline contradiction — **by asking, not by assuming** | p.1 Project Overview says Sunday **11:59 AM CT**; p.12 Submission Requirements says Sunday **11:59 PM CT**. Ticket closes when a grader's answer is recorded in `SUBMISSION-PLUGFORGE.md` with the date asked, the channel, and the answer. Until then the board plans against the **earlier** time and says so explicitly — an unrecorded assumption that PM wins is a twelve-hour bet on a document that disagrees with itself | — | p.1, p.12 | PF-781 |
| PF-783 | ◐ Repo state audit — public, and every per-slice branch still on the remote | Two mechanical checks recorded with output: (a) the repo resolves over HTTPS from a logged-out client, not just from an authenticated one; (b) `git ls-remote --heads origin 'pf/*'` returns a branch for every slice declared in every lane file's `## Slices` section, count matching. p.12 grades *"Public; per-slice branches preserved"* — a merged-and-deleted branch is unrecoverable evidence loss **Re-measured 2026-08-15. Both halves now pass — but on two different remotes, and that is the finding.** (a) **Public:** `curl -o /dev/null -w '%{http_code}'` with no cookies and no credentials → `github.com/joshdrochon/ship` **200** ✅; `labs.gauntletai.com/joshrochon/ship` **302 → /users/sign_in** ❌. The 200 you get with `-L` is the sign-in page, not the repo. GitLab is the graded remote and it is **not public to a logged-out client**. (b) **Branches preserved on `origin`:** `git branch --list 'pf/*' \| wc -l` → **147**; `git ls-remote --heads origin 'pf/*' \| wc -l` → **147**; set-difference **empty in both directions**. This supersedes the S1a row's 119/11/5 and §1's 127/127 — both were taken before the final pushes. **On `github`, the public remote: 0 `pf/*` branches.** Not an auth failure — `git ls-remote --heads github` succeeds and returns 7 heads, none of them `pf/*`; GitHub still carries Week 5 (`main` at `5455f4e`). **So p.12's two words are satisfied by two remotes and by neither one alone: the public repo has no per-slice branches, and the repo with all 147 branches is not public.** Left ◐ deliberately — the resolution is a decision (make GitLab public if the platform allows it, or push `pf/*` to GitHub, or tell the grader which remote to look at), not a measurement. Recorded in `SUBMISSION-PLUGFORGE.md` §1. | SUB:GitHub Repository | p.12 | PF-025 |
| PF-784 | ◐ PR-body compliance sweep across every merged PR | For each merged PR: the body names the acceptance criterion the slice advances (ticket IDs **and** criterion text) and confirms the fitness test passed — and the named fitness test resolves to a real test or check that has a passing run. PF-026 enforces the *sections exist* at PR time; this is the submission-time check that the content is true. Output is a table of PR number → criterion → fitness test → pass evidence, with zero unresolved rows **Swept 2026-08-14 — `docs/pr-compliance-sweep.md`. Left ◐, not ☑: the ticket asks for zero unresolved rows and there are 11.** 55 of 66 slices name both their tickets and an artifact that ran; 9 name tickets and describe verification in prose without naming a test file, ratio or exit code, so a reader cannot re-run the claim; 2 name no ticket at all and both are mine from the final evening. Rewriting those messages would mean rewriting merged history, which the branch policy forbids for the same reason this sweep exists. Also records the two wrong measurements taken first — merge commits (3/150, wrong artifact) and merge-base..branch (131/135, reads ancestor commits for a fully merged branch) — before merge parents gave the right unit. | SUB:GitHub Repository | p.12 | PF-024, PF-026 |
| PF-785 | ◐ Branch-name and slice-mapping reconciliation | Every remote branch matches `pf/LNN-<slug>` with `LNN` resolving to a real lane file, and the slice map is bijective: each declared slice has exactly one merged PR, each merged PR maps to exactly one declared slice. Orphans in either direction are listed and resolved before Final — a PR with no slice cannot name what it advances, and a slice with no PR is undelivered work the index will claim as Ready **Measured 2026-08-15, and the map is not bijective. Left ◐ with the numbers, because closing it means deciding what to do about 58 rows, not measuring them again.** All counts from `origin` (github has no `pf/*` — see PF-783). **Naming:** 146 of 147 remote branches match `pf/LNN-<slug>`; the one exception is `pf/integration`, the trunk, which is not a slice. LNN values span L00–L26. **`L00` resolves to no lane file** — no `tickets/plugforge/lane-00-*.md` exists, and two branches use that prefix (the guard-verification and protection-probe pair). L01–L26 all resolve. **Bijection:** 139 slice rows declared across all lane files, **135 unique** branch names — the 4-row gap is `lane-05-oauth-device.md`, which carries two tables under `## Slices` (a *Landed* table plus the original planning table) and double-declares S1–S4. Of those 135: **112 have a branch on origin**, **23 have no branch anywhere** (whole-lane misses: L20 5-of-6, L22 4-of-5; also all four `pf/L26-*` planning names, since this lane shipped under different branch names), and **35 origin branches are not a declared slice**. Several orphans are renames the lane file never caught up with — L24's browser-pkce branch against a declared *-demo* suffix, L16's ceilings branch against a declared scenarios-and-ceilings — which is a cheaper class of fix than a missing branch and should be separated from it before anyone counts 58 problems. Full lists in `SUBMISSION-PLUGFORGE.md` §1b. | SUB:GitHub Repository | p.12 | PF-023, PF-783 |
| PF-786 | ☐ Nine required sections present, correctly named, and inside the length limit | A check asserts `docs/architecture.md` carries all nine headings from the p.12 Section/Content table — Module Layout, SOLID Rationale, Composition Root, Public/Internal Boundary, OAuth Flows, Webhook Pipeline, SDK Surface, Agent-as-Citizen, Failure Modes — and no required section is missing. p.13 caps it at *1–2 pages*: the ticket states how pages were measured (rendered output, not source lines) and the resulting number. The file today is 192 source lines with five mermaid diagrams, so the measurement is not optional | SUB:Architecture Document | p.12, p.13 | — |
| PF-787 | ☐ Per-section content contract — each section carries the artifact its row demands | Nine checklist rows, each citing the line where the demanded element appears: Module Layout = tree covering all eight named modules with one sentence each; SOLID = one paragraph per principle **with a file path**; Composition Root = annotated pseudo-code **plus** the in-memory test wiring as a sibling; Public/Internal Boundary = a sequence diagram; OAuth Flows = two sequence diagrams marking where PKCE is validated and where rotation happens; Webhook Pipeline = the full chain with the signature site and Idempotency-Key origin marked; SDK Surface = stable vs pre-1.0 explicitly marked; Agent-as-Citizen = before/after diagram with the audit-log payoff marked; Failure Modes = one paragraph each for all four named scenarios. A section present but missing its demanded element fails the row | SUB:Architecture Document | p.12 | PF-786 |
| PF-788 | ☑ Reconcile G1 — the doc claims the agent's OAuth app is **seeded by migration** | `docs/architecture.md` line ~178 asserts migration seeding; the repo seeds through `seed.ts`. p.17 only *asks* the question, so the commitment is ours and it is new work. Ticket closes when one of two states is true and recorded: the migration seeding shipped, or the doc says `seed.ts` — plus, either way, the doc answers p.17's real question (*what guarantees the app exists in deployed environments*). Leaving both documents in disagreement is not a third option **Closed 2026-08-15 — G1 was never a defect, and the re-verification says so from the code rather than from the earlier note.** The doc's claim is `db:migrate` (**not** a numbered migration): `docs/architecture.md:128-130`. The code agrees at `api/src/db/migrate.ts:125` — `const seededApps = await seedPlatformApps(pool);`, imported from `./platformApps.js` at `migrate.ts:14`. There is no `seed.ts` path for these rows at all. p.17's real question (*"How is the agent's app seeded — at boot, via a migration, manually in dev? What guarantees it exists in deployed environments?"*, grep-verified to `page-17.txt`) is answered in two places: `architecture.md:129-130` (*re-applied on every deploy rather than once per database*) and `docs/architecture-appendix.md:761-769`, which spells out **why** it is deliberately not numbered — `migrate.ts` skips anything already in `schema_migrations`, so a numbered file runs once per database and a **rotated** secret would never be written. The upsert is idempotent via `ON CONFLICT (client_id) DO UPDATE` (`api/src/db/platformApps.ts:326`). Re-runnable: `grep -n seedPlatformApps api/src/db/migrate.ts`. | SUB:Architecture Document | p.17 | PF-787 |
| PF-789 | ☑ Reconcile G2 — the audit-field list omits `request_id` | `docs/architecture.md` line ~18 lists *timestamp, client_id, user_id, route, scope, status, latency*. p.18 names `request_id` explicitly and the `ApiError` interface (p.7) already carries it. Ticket closes when the doc's field list, the shipped audit-row columns, and p.18's list agree — or when the omission is stated as a decision with its reason. Silent divergence between a graded doc and a graded interface is the failure mode **Closed 2026-08-15 — all three lists agree, re-verified independently.** Doc: `docs/architecture.md:20` now reads *"timestamp, app client_id, user_id, route, scope, status, latency, request_id"*. Shipped: `api/src/db/migrations/057_public_api_calls.sql:46` — `request_id text NOT NULL` — plus its own index at `:79-80`. PRD: `page-18.txt:17` names *"(route, status, latency, scope used, app, user, request_id)"* and `page-7.txt:5` carries `request_id: string;` on `ApiError`; both grep-verified, neither derived from `full.txt`. **One residual, non-blocking and recorded rather than hidden:** the shipped table also has `method text NOT NULL` (`057:54`), which neither the doc's list nor p.18's list mentions. p.18 is the graded list and the doc matches it exactly, so this is a doc that is a strict subset of the schema, not a contradiction. Re-runnable: `grep -n request_id api/src/db/migrations/057_public_api_calls.sql`. | SUB:Architecture Document | p.18, p.7 | PF-787 |
| PF-790 | ◐ Reconcile G3 — *"the Terraform delta is env-group entries only"* | L21 PF-647 owns the edit; this ticket owns the verification. `terraform/render/main.tf` declares no `render_env_group` — env vars are an inline map on the web service. Closes when a reviewer diffing the Deployment Topology paragraph against the shipped config finds no discrepancy, and the zero-new-resources claim it sits next to is sourced from the PRD (PF-621) rather than from our own document **Clause 1 verified and passing; clause 2 measured and FAILING — left ◐ 2026-08-15.** (1) The env-group defect is gone: `grep -rn render_env_group terraform/` returns **zero hits** (the only matches in the repo are this ticket's own text and lane-21's), the Render env vars are still an inline `env_vars = merge(…)` on the web service at `terraform/render/main.tf:237`, and the Deployment Topology paragraph no longer describes Render at all — `docs/architecture-appendix.md:865-871` describes the applied AWS topology (EB `ship-api-prod`, Aurora Serverless v2, NAT gateway, the three-link security-group chain, the two IAM roles) and says in its first sentence that it supersedes Render. Diffed against `terraform/*.tf`: no discrepancy found in that paragraph. (2) **The adjacent zero-new-resources claim is not sourced from the PRD and is no longer true as written.** `appendix:871` still asserts *"PlugForge's own must-ship surface still adds no AWS resources."* Neither p.5 nor p.2 — the only two pages naming Terraform, both grep-verified — contains any zero-new-resources statement, so the claim is ours, which is exactly what this clause was written to catch. It is also contradicted by shipped config: `terraform/platform-apps.tf` declares **six** resources that exist only for PlugForge (`random_password` ×3 at `:41,:46,:51`; `aws_ssm_parameter` ×3 at `:56,:67,:78`), and its own header names PF-625/PF-630 and the three first-party OAuth apps as the reason. Lane-21's PF-647 anticipated this — it says the claim *"is now false and comes out with it"* — but the sentence is still in the file. **The fix is one sentence in `docs/architecture-appendix.md`, which this lane does not own; routed to L21 rather than edited here (see the lane header's own scope-creep warning on G3).** | SUB:Architecture Document | p.5, p.12 | PF-622, PF-787 |
| PF-791 | ☑ Reconcile G4 — the doc never names the agent's OAuth grant type | It says *"first-party OAuth app"* and stops. p.17 asks which flow — Authorization Code, Device Grant, or Client Credentials (RFC 6749 §4.4) — and demands the choice be defended; p.13's interview topics put the agent's citizenship on the record. Closes when Agent-as-Citizen names one grant, cites the RFC section, and states the rejected alternatives, so the question is answerable from the document alone. Tracked as D5 in `lane-99-unassigned.md` — the decision is the user's; the recording is this ticket's **Closed 2026-08-15 — the document now answers p.17 without help.** All three demanded elements are in Agent-as-Citizen at `docs/architecture.md:126-128`: **one grant** named (Client Credentials), **the RFC section cited** (RFC 6749 §4.4), and **both rejected alternatives named with the reason** (*"chosen over Device Grant and Auth Code because it runs server-side on a schedule with no user present to consent, and it can keep a secret"*). The same three appear as a row in the OAuth Flows table at `:85`. p.17's question is grep-verified to `page-17.txt` (*"Which OAuth flow does the agent use — Authorization Code, Device Grant, or Client Credentials (RFC 6749 §4.4) for first-party machine-to-machine? Defend the choice."*), and p.13 is the interview-topics page. The choice is also load-bearing in code, not just prose: the agent app is seeded `isFirstParty: true` and deliberately confidential (`api/src/db/platformApps.ts:117-121`), and PF-809's live capture minted its token with `grant_type=client_credentials` and got no `refresh_token` back, per RFC 6749 §4.4.3. | SUB:Architecture Document | p.17, p.13 | PF-787 |
| PF-792 | ☐ Reconcile the two diagrams that describe composition the code does not yet have | Public/Internal Boundary names `documentService (utils/document-crud.ts)` as the shared seam — F8: that file has no create/update/delete and write SQL is inline in the route files. Composition Root's pseudo-code mounts `/api/v1/openapi.json` **after** `app.use('/api/v1', v1)` and under a blanket `router.use(bearerAuth)` — F11: as drawn, the spec route 404s and 401s. Closes when both diagrams match the shipped wiring, including middleware order, and a reader tracing either diagram against `api/src/app.ts` lands on real code **Half resolved, half still wrong — left ◐ 2026-08-15, and the ticket's F8 premise is now stale.** **F8 (Public/Internal Boundary) — RESOLVED, and the premise was overtaken by the build.** The diagram no longer names `utils/document-crud.ts`; `docs/architecture.md:59-73` names a generic `document service`, and that resolves to a real shared seam: `api/src/routes/documents.ts:12` imports `documentService` from `../services/documents.js`, and `api/src/platform/api/v1/documents/routes.ts:44-48` imports `createDocumentService` from the same module. Both surfaces genuinely call one service, so the diagram's load-bearing claim is a fact. The publish site is inside the service, not the route — `services/documents.ts:459` (`document.created`) and `:511` (`document.deleted`) — which is what makes the `SVC->>BUS` arrow accurate and satisfies p.3's *"domain layer publishes on writes, never the route layer."* F8's own observation still holds against `api/src/utils/document-crud.ts` (no create/update/delete exports; it is association/history helpers), but that file is no longer what any diagram points at. **F11 (Composition Root) — STILL WRONG, and the code says so in its own words.** The sketch is now in `docs/architecture-appendix.md:81-83` and still reads `app.use('/api/v1', v1);` **followed by** `app.get('/api/v1/openapi.json', serveGeneratedSpec());`. `api/src/platform/openapi/route.ts:9-11` calls that out verbatim — *"`app.get('/api/v1/openapi.json', serveGeneratedSpec())` mounted AFTER `app.use('/api/v1', v1)`. Both halves of that are wrong against the router L08 [built]"* — and `:21` names the second failure, `router.use(deps.bearerAuth)` blanketing every path below it. The shipped wiring is the opposite: the spec is mounted **inside** the v1 router through the `mountUnauthenticated` seam, above `bearerAuth` and above the catch-all — `api/src/app.ts:386`, `mountUnauthenticated: mountOpenApiSpec(generatePublicOpenAPIDocumentOrDie())`, handler at `platform/openapi/route.ts:118-120` on `OPENAPI_SPEC_PATH = '/openapi.json'` (`:59`). A reader tracing the sketch lands on a 404-then-401 that does not exist; the live route returns **200 uncredentialed** (PF-813). **The fix is three lines of the sketch in `docs/architecture-appendix.md`, which this lane does not own — not edited here.** | SUB:Architecture Document | p.12 | PF-403, PF-365, PF-366, PF-213 |
| PF-793 | ◐ As-built sweep of the load-bearing specifics | After the build lands, every concrete value the doc asserts is checked against shipped code and named in a table with its source line: hash algorithm, retry-ladder values, the exact signed string and header format, the five `kind` error-union members, the scope names, the cursor envelope keys, the SDK footprint budget, the signature tolerance seconds. Any divergence is resolved in one direction and the ticket records **which document moved**. This runs after L25 PF-773 and in the opposite direction — PF-773 aligns the Pre-Search to the architecture doc; this aligns the architecture doc to the code **Sweep RUN 2026-08-15, all nine named values plus seven more; full table with every source line in `SUBMISSION-PLUGFORGE.md` §4b.** Left ◐ because the criterion also demands *"any divergence is resolved in one direction"* and the four divergences all resolve inside `docs/architecture.md` / `docs/architecture-appendix.md`, which this lane does not own this run — they are named and routed, not resolved. **Twelve values match the code exactly:** hash algorithm (`SHA-256`, unsalted, hex — `api/src/platform/apps/secrets.ts:122`) over a 32-byte CSPRNG secret (`:73`); retry ladder `[1, 4, 16, 60, 300, 1800]` (`platform/webhooks/retry.ts:62`) with ±10% bounded jitter (`:77`); the signed string `` `${t}.` `` ++ rawBody (`webhooks/signer.ts:91-93`) and the header `Ship-Signature` / `/^t=\d+,v1=[0-9a-f]{64}$/` (`:67`, `:88`); tolerance **300 s** on both sides of the wire (`signer.ts:79`, `sdk/src/webhooks.ts:50`); the five `kind` members `auth · rate_limit · not_found · validation · server` in that order (`sdk/src/errors.ts:45-51`) and the six wire codes behind them (`:65-72`); the agent's read-only scope triple (`api/src/db/platformApps.ts:117`); the Idempotency-Key derivation `${eventId}:${subscriptionId}` (`platform/webhooks/pipeline.ts:180-182`) read back on replay rather than recomputed (`webhooks/replay.ts:112`); `FileTokenStore` mode `0o600` (`sdk/src/auth/fileTokenStore.ts:39`). **Four divergences found, none previously recorded:** (a) **cursor envelope** — both docs say `{id, timestamp}` (`architecture.md:77`, `appendix:137`) but the shipped `CursorPayload` has a **third** key, `resource`, and it is load-bearing, not cosmetic — it is what makes a `/documents` cursor a `validation_failed` against `/issues` instead of a plausible wrong page (`platform/api/v1/pagination.ts:83-98`, minted at `:296`); (b) **SDK footprint** — `appendix:591` says *"measured: 160.4 KB gzipped"*; `sdk/size-report.json` says **213 786 bytes = 208.8 KiB**, still inside the 256 000-byte budget (`sdk/src/installSize.test.ts:22`) but the published figure is stale by ~48 KB; (c) **agent scopes contradict between our own two documents** — `architecture.md:132` says read-only (correct, matches `platformApps.ts:117`) while `appendix:765` lists `issues:write`, which the code does not seed and which would falsify PF-809's read-only proof if a grader read it; (d) **demo app scopes** — `appendix:767` omits `webhooks:manage`, which `platformApps.ts:184` does seed and `scopes.ts:64` does register. **One imprecision, flagged not counted:** `architecture.md:105` lists all six retry rungs, but `MAX_ATTEMPTS = 6` consumes only five waits (`retry.ts:72,75`), so the 30 m rung is unreachable — `appendix:64-69` states this and the main document does not. | SUB:Architecture Document | p.12, p.13 | PF-787, PF-773 |
| PF-794 | ☑ Scaffold `docs/ai-cost-analysis.md` — three halves, and the headline claim proven | Three sections named from the p.13 row itself: **tracked dev spend**, **production projections table**, **explicit assumptions**. The document opens by proving rather than asserting p.9's headline discipline — *"the platform itself does zero AI work"* / p.11's *"The platform never invokes the LLM"* — with a named mechanical check (no module under `api/src/platform/**` imports an LLM client) and its output pasted in. An assertion with no check behind it is the one claim in this document a grader can falsify in thirty seconds **Done 2026-08-15.** `docs/ai-cost-analysis-plugforge.md` — separate file because `docs/ai-cost-analysis.md` is Week 5's and cites the ShipShape brief. All three halves present. The headline claim is proven rather than asserted, as this ticket demands: the document OPENS with `grep -rlE "@langchain\|anthropic\|openai" api/src/platform/ \| wc -l` returning **0** against **10** for `agent/src`. **Evidence corrected 2026-08-15 — this row previously asserted *"Marginal AI spend $0.00, measured"* and that claim is retracted in the document it produced.** What is true: **production** spend is $0.00, established by the *environment* rather than by the AWS bill — the deployed environment carries four application variables (`AWS_REGION`, `ENVIRONMENT`, `NODE_ENV`, `PORT`) and neither `ANTHROPIC_API_KEY` nor `BEDROCK_ENDPOINT`, so `selectProvider()` falls to `bedrock` with no credential, the model client fails to initialise, and judgement degrades to `ai_unavailable` (`agent/src/llm/client.ts:214–228`). What was wrong: the AWS Cost Explorer query cannot see the provider actually in use. `agent/src/llm/client.ts:181` selects the **direct Anthropic API** and `:249` constructs `ChatAnthropic`; direct Anthropic calls bill to Anthropic, never to AWS, so `aws ce get-cost-and-usage` is conclusive for the Bedrock path and for nothing else. **Development spend — which is what p.9 actually asks about — is real and unrecorded, and the document states it as an explicit gap rather than a zero:** `grep -c '^ANTHROPIC_API_KEY=' .env agent/.env` → `1` and `1`, and `start.sh:207` deliberately does not point `BEDROCK_ENDPOINT` at the mock, so a local `./start.sh` resolves to `'anthropic'` and issues a real billed call. Bounded, not totalled — model `claude-opus-4-5` (`client.ts:107`), 1 call per judged turn, `MAX_TOKENS` 2,048 (`client.ts:113`), scans run in the window **unrecorded**. Projection constants are each cited to a line and the arithmetic re-checked against the table as shipped — 100-user tier `5,000 × 30 × 1,115` = 167 MB healthy, `× 6` = 1.0 GB worst. The `3000×6×30×1115 = 602 MB` figure this row used to quote matches no row in the current table and is dropped with the rest of the stale text. Re-runnable: `grep -n "ANTHROPIC_API_KEY) return\|new ChatAnthropic" agent/src/llm/client.ts`. | SUB:AI Cost Analysis | p.13, p.9, p.11 | — |
| PF-795 | ◐ Dev spend — LLM during the Epic 7 rewire, with before/after token volume | Per-day spend recorded across the rewire days (p.9: *"track per-day spend while migrating direct service calls to SDK calls"*), plus the measurement that answers p.9's actual demand: token counts per agent turn over the same fixture prompt set, flag-off vs flag-on, stated as absolute numbers and a percentage delta. The claim *"the rewire does not change token volume"* is then either confirmed with that delta or refuted with it — a rewire that moved token volume and said nothing is the failure this bullet exists to catch **Left ◐ 2026-08-15 — an honest gap is not the measurement this criterion asks for, and the criterion is what closes the ticket.** What landed: production spend established at **$0.00** by the environment check (no model credential in the deployed environment, `agent/src/llm/client.ts:214–228`), and the development gap **bounded** rather than zeroed — model `claude-opus-4-5` (`client.ts:107`), **1** call per judged turn (`judge.ts:147`, `answer.ts:65`), `MAX_TOKENS` **2,048** (`client.ts:113`), `MAX_ATTEMPTS` **3** (`client.ts:117`), scans run on a dev machine in the window **unrecorded**. The document refuses to report a zero it cannot measure, and names why no instrument here can: the spend is on the direct Anthropic path (`client.ts:181`, `:249`), it does not reach AWS, the agent writes no token ledger, and the only authoritative source is the Anthropic Console — outside CI and outside this document's re-runnable standard. **Two clauses of the criterion remain unmet.** (a) *"Per-day spend recorded across the rewire days"* — there is no per-day series and no total; the gap is stated, not filled. (b) *"token counts per agent turn over the same fixture prompt set, flag-off vs flag-on, stated as absolute numbers and a percentage delta"* — no delta exists. The document argues the claim **by construction instead**: `SHIP_AGENT_VIA_SDK` is read in exactly one place, `agent/src/composition.ts:44`, and `grep -rn "SHIP_AGENT_VIA_SDK" agent/src/llm/` returns **zero hits**, so the rewire moved the data path and touched nothing in the LLM path. That is a sound argument and it is not the delta the criterion demands — recorded as ◐ rather than ticked, because *"either confirmed with that delta or refuted with it"* is the clause and neither happened. Closing needs a fixture prompt set run twice with token counts captured; the per-day half may never close in-repo. | SUB:AI Cost Analysis | p.9 | — |
| PF-796 | ◐ Dev spend — CI compute across the three metered jobs | Three separate numbers, none folded into a total: **TTFE drill** — measured seconds per run, timed on Day 1 *and* at Final (p.9 asks for the Day-1 number explicitly), × runs per PR × PRs per day → weekly CI minutes and dollars at the runner's posted rate; **OAuth Playwright browser launches** — an instrumented count per suite run, not an estimate, converted to compute minutes; **OpenAPI generation and validation overhead** — milliseconds per CI run and its share of the CI bill, because p.9 asks for *a number rather than a hand-wave*. A section that reports only the total fails all three **Left ◐ 2026-08-15 — three separate numbers exist and none is folded into a total, but two of the three carry a named sub-item that is still missing.** **TTFE — closed.** Day-1 is **pipeline 19893** with its constraint named rather than papered over: the `ttfe` job entered `.gitlab-ci.yml` on 2026-08-14 (`git log --oneline --reverse -S"ttfe" -- .gitlab-ci.yml` → `06cdc49`), so Day 1 of the drill is not Day 1 of the week and no earlier figure exists to report. Re-verified this pass against the API, not taken on trust: `glab api "projects/joshrochon%2Fship/pipelines/19893/jobs?per_page=100"` returns job **65064** `ttfe` **69.87 s** and **65065** `ttfe-controls` **32.90 s**, created `2026-08-14T15:15:52Z` — subtotal **102.8 s**, exactly the document's figure. Final is pipeline 20223 at 50.4 s. Weekly: **35.1 min billed**, *54.8 min projected* at 62 pipelines × 53.0 s median, the two deliberately kept apart because the job landed mid-window. Weekly CI overall is **59.4 h summed across all 62 window pipelines** — summed per-job, not extrapolated from one pipeline, and the document shows the earlier 3.3× error it replaces. **No dollar figure, and that is a correction rather than an omission:** all 1,072 jobs ran on runner **198**, `is_shared: false`, `runner_type: project_type` — GitLab's $0.008/min prices *shared* runners, so the rate is wrong in kind, marginal cost is ~$0, and §4 carries the counterfactual (~$28.50/week if moved to shared runners). **OAuth — count landed, conversion refused.** `e2e/oauth-pkce.spec.ts` holds **5** tests (`grep -n "test(" e2e/oauth-pkce.spec.ts` → 5 sites, verified) against **888** executed in job **66343**, whose duration `glab api ".../jobs/66343"` confirms as **2,275.878947 s** — **0.56% by count**. The criterion also says *"converted to compute minutes"* and the document declines: Playwright's CI reporter prints completions, not per-spec durations, and four workers interleave, so the trace cannot attribute wall time — and one of the five is a *"P95 over 20 runs"* benchmark that breaks any equal-cost assumption. Sound refusal, undelivered number. **OpenAPI — half.** `pnpm openapi:public` **1.3 s** and the `openapi-freshness` job **24.4 s** are both stated; *"its share of the CI bill"* is **not computed** — the weekly breakdown attributes `e2e` 63%, `test` 7%, `regression-budget` 2%, `ttfe`+`ttfe-controls` 1.0%, and `openapi-freshness` appears in no row. That share is computable from the same `jobs/*.json` snapshot and is the cheapest remaining item on this ticket. | SUB:AI Cost Analysis | p.9, p.8 | PF-371 |
| PF-797 | ☑ Dev spend — dev-portal storage and egress at demo volume | Sized, not guessed: delivery-log rows produced per drill run × bytes per row × drill runs over the week, plus portal egress at the expected demo volume. p.9 notes the logs *"grow with every drill run"*, so the number is a function of drill frequency and the ticket states which frequency it used. Feeds PF-801's retention arithmetic — the two must use the same bytes-per-row figure **Closed 2026-08-15 — sized per run, and the third factor is proven structurally zero rather than guessed at.** **Rows per drill run: 3**, decomposed rather than counted off — `pnpm drill ttfe` runs three tests and the third fans out to two subscriptions, because test 1 creates a `document.created` subscription (`ttfe.drill.ts:218`) and never deletes it while `webhooks tail --listen` creates a second (`webhooksTail.ts:207`), so one `docs create` (`:475`) fires both. Fanout selects every active subscription for the workspace and event type — `WHERE workspace_id = $1 AND event_type = $2 AND active = true` (`pgSubscriptionRepo.ts:147`) — and both clients land in the same workspace by construction, not by luck: subscriptions are stamped `workspace_id: who.workspaceId` (`api/v1/webhooks/routes.ts:331`) and both tests approve through the same device grant against migration 041's hard-coded owner (`ttfe.drill.ts:166`, `:435`). The four `--controls` are negative by construction and contribute nothing. **Bytes per row: 1,115** — `BYTES_PER_ROW = 160 + 75 + 20 + 80 + 280 + 500` re-verified this pass at `api/src/platform/webhooks/retention.ts:61`, and it is the **same single figure** §2, §3 and PF-801 use, which is the clause this ticket shares with PF-801. Per run: **3,345 B** first-attempt, **20,070 B** worst case at `ATTEMPT_MULTIPLIER_CEILING = 6` (`retention.ts:40`). **The `× drill runs over the week` factor is zero, permanently, and the proof is in the harness rather than in an outage:** `scripts/ttfe/harness.ts:149` issues `DROP DATABASE IF EXISTS "<name>"`, **`:154` re-queries `pg_database` for that name and throws if a row comes back** — the drill's own teardown assertion — and the default path never touches a shared server at all, `:165–169` starting `new PostgreSqlContainer('postgres:16')` and disposing it with `container.stop()`. All three lines re-read this pass. `ttfe.negative.drill.ts:133` asserts the same property by name. So the criterion's *"function of drill frequency"* is answered more strongly than by picking a frequency: persistent accrual is zero at **every** frequency, and would stay zero on a runner with Docker. The per-run figure is retained as a **peak** because p.9 asks the log to be sized at demo volume and a demo reads the rows while the run is live. **Egress sized and re-derivable:** the portal reads at `DEFAULT_PAGE_SIZE = 25` (`api/v1/pagination.ts:49`) over a projection that drops `raw_body`, and the document now pastes the full `/tmp/wire.js` script instead of eliding it — **633 B** per success row, **886 B** per failed row with a full 256-char excerpt, **15,879 B** per 25-row page. One drill run's three rows are **~1.9 kB**; a demo session reloading twenty times moves **~38 kB**. Those replace the 670 / 944 / 16,842 figures the elided version reported, which rested on field widths nobody could check. | SUB:AI Cost Analysis | p.9 | PF-794 |
| PF-798 | ◐ Production projections table — state whether each figure is reproduced or derived | p.9 supplies a four-tier table (100 / 1k / 10k / 100k users; API calls/day, webhook deliveries/day, agent LLM calls/day, est. cost/month). The deliverable reproduces the tier structure **and** labels every cell `[PRD]` or `[derived]`. Every `[derived]` cell shows its arithmetic from PF-799/PF-800's assumptions. Where our derivation disagrees with p.9's figure, the disagreement is shown side by side and explained — never silently overwritten. A table copied wholesale carries no assumptions and cannot be defended in the interview; a table that quietly contradicts the PRD's own numbers reads as an error rather than a finding **Left ◐ 2026-08-15 — everything the criterion asks for is present except the one thing it emphasises with an `and`.** **Tier structure reproduced:** §2 carries p.9's four tiers and all four of its columns (API calls/day, webhook deliveries/day, agent LLM calls/day, est. cost/month), plus two storage columns that are ours. **Arithmetic reproducible by hand** from the constants table, each cited and re-verified this pass: `BYTES_PER_ROW` 1,115 (`webhooks/retention.ts:61`), `RETENTION_DAYS` 30 (`:76`), `ATTEMPT_MULTIPLIER_CEILING` 6 (`:40`), `RAW_RETENTION_DAYS` 30 (`audit/retention.ts:51`), `ROLLUP_RETENTION` indefinite (`:54`). Checked by hand: 100-user tier `5,000 × 30 × 1,115` = **167.25 MB** ✅ against the printed 167 MB, `× 6` = **1.0035 GB** ✅ against 1.0 GB, audit `20,000 × 30 × 400` = **240 MB** ✅. **Disagreement shown side by side rather than overwritten:** the *"Where our model disagrees with p.9's estimate"* paragraph puts the 1 TB worst case against p.9's $500–1,500 and says the 6× is an operational risk reachable by one popular subscriber going down for a day, not a padding factor. **What is missing is the cell labelling, and the criterion bolds the `and` that demands it.** No cell in §2 carries `[PRD]` or `[derived]`. Provenance is stated once in prose above the table — *"The columns below are p.9's; the two storage columns are ours"* — which is determinate at column level, and a reader can resolve every cell from it. It is still not the per-cell marker the criterion names, so this is ◐ rather than ☑. Closing it is a formatting pass over six columns × four rows in a file this lane does not own this run. **The lane's own note at the foot of this file flags reproduce-vs-derive as *"the single softest call in the lane"* — this ◐ does not re-open that call**, it only records that the chosen reading was implemented in prose rather than in labels. | SUB:AI Cost Analysis | p.9 | PF-794 |
| PF-799 | ◐ Assumption — webhook fanout ratio, stated explicitly | One ratio: webhook deliveries triggered per write operation, backed by the average number of subscriptions per event type at each tier (p.9 asks for exactly that decomposition and says *"State this explicitly"*). The projections table's deliveries/day column equals that ratio × writes/day and the arithmetic is reproducible by hand from the stated numbers. Reconciles with L25 PF-753's fanout answer — if the two disagree at Final, the Pre-Search is the earlier document and this one moves **Left ◐ 2026-08-15 — a ratio is stated explicitly, and its denominator is the wrong quantity.** §3 states *"0.25 deliveries per write operation at every tier, i.e. p.9's ~5,000 deliveries against ~20,000 API calls/day"*, and flags the ratio as the load-bearing optimistic simplification — fanout is per **subscription**, so a platform that succeeds acquires more apps subscribing to the same popular event types and the curve bends upward. That much satisfies *"State this explicitly"*. **Three clauses do not close.** (a) **The denominator is API calls, not writes.** 5,000 ÷ 20,000 = 0.25 only if every API call is a write; the criterion says *"per write operation"* and *"deliveries/day column equals that ratio × writes/day"*. p.9's column is API calls/day, most of which are reads, so the true per-write ratio is larger by whatever the read/write split is — and that split is not stated anywhere in the document. The number is arithmetically consistent with p.9's table and mislabelled against the criterion, which is the same defect class as PF-794's. (b) **The decomposition p.9 asks for is absent.** *"given the average number of subscriptions per event type at each tier"* — the document says only that this average *"does not grow with the tier"*; it never gives the average a value, so the ratio is asserted at the top level rather than built from N. (c) **No reconciliation with L25 PF-753 is recorded**, and the two documents frame the quantity differently: `PRESEARCH-PLUGFORGE.md:82` answers *"Fanout is 1 event × N matching active subscriptions × up to 6 attempts = at most 6N outbound requests per event"* — a per-event decomposition in N with the retry multiplier folded in — while the cost analysis gives a per-call scalar with retries handled separately in §2's worst-case column. Not a contradiction, but the tie-break the criterion names (*"the Pre-Search is the earlier document and this one moves"*) has not been applied to either. | SUB:AI Cost Analysis | p.9 | PF-798, PF-753 |
| PF-800 | ◐ Assumption — agent active rate, with its sensitivity | Two numbers: fraction of users using agent features on a given day, and average agent turns per active user. The agent-LLM-calls/day column equals users × active rate × turns per user and the arithmetic checks against PF-798's table. p.9–p.10 states the projection *"bends on this assumption, not on platform traffic"* — so the section also states what the monthly cost does at 2× the assumed rate. An assumption whose sensitivity is unstated is a number, not an assumption **Left ◐ 2026-08-15 — the sensitivity clause is unmet and one of the two numbers contradicts its own worked arithmetic.** **(a) The two numbers are stated but disagree with each other in the same sentence.** §3 reads *"Assumed: 5% of users use agent features on a given day, at ~1 turn per active user"* and then, in the parenthetical immediately after, *"100,000 users → 5,000 active → ~50,000 calls/day requires ~10 turns each; at 100 users → 5 active → ~50 calls/day, ~10 turns each."* The parenthetical is the correct arithmetic — 100,000 × 5% = 5,000 active, 50,000 ÷ 5,000 = **10** turns — so the bolded headline figure of **~1 turn** is wrong by 10×. The criterion is *"Two numbers"* and *"the arithmetic checks against PF-798's table"*; the arithmetic checks, the stated number does not match it. A grader reading the bold text and the table gets 1 and 10 for the same quantity. **(b) The sensitivity is not stated.** p.10's *"Cost projection bends on this assumption, not on platform traffic"* is quoted and agreed with, and the base case is priced — 100,000 users at ~50,000 calls/day and a ~4k-token turn puts the agent at **$3,000–6,000/month on its own**, several times p.9's whole $500–1,500 for that tier, with the non-conflict explained (p.9 attributes LLM cost to the agent app's user-driven sessions, not the platform). But the criterion asks *"what the monthly cost does at 2× the assumed rate"* and no 2× figure appears — a doubled active rate is 10% → 100,000 calls/day → ~$6,000–12,000/month, and the document does not say so. **One genuine strengthening is recorded and does not close the ticket:** the agent's read-only scopes `documents:read, issues:read, sprints:read` (`api/src/db/platformApps.ts:117`) bound the assumption as a cost property, not only a security one — the agent cannot write, so it cannot trigger a webhook, so it cannot grow PF-799's fanout. Both (a) and (b) are fixes inside `docs/ai-cost-analysis-plugforge.md`, which this lane does not own this run — named and routed, not edited here. | SUB:AI Cost Analysis | p.10, p.9 | PF-798 |
| PF-801 | ☑ Assumption — storage retention, **both** windows, each with why it is set there | p.10 demands delivery-log rows × retention days × bytes per row, *plus* audit-log rows, and *"State both retention windows and explain why each is set there."* So: two windows as day counts, two bytes-per-row figures (bytes-per-row shared with PF-797), two monthly storage numbers, and a reason per window that is a constraint rather than a preference — replay usefulness and DLQ tail for deliveries, incident forensics and the agent-went-through-the-front-door proof for audit. Identical windows are allowed; an unexplained window is not **Closed 2026-08-15 — every element the criterion enumerates is present, and the shared-bytes clause with PF-797 holds.** **Two windows as day counts:** delivery log **30 days** — `RETENTION_DAYS = 30`, re-verified at `api/src/platform/webhooks/retention.ts:76` — and audit raw **30 days**, `RAW_RETENTION_DAYS = 30` at `api/src/platform/audit/retention.ts:51`. Identical, which the criterion explicitly allows, and each carries its own distinct reason. **Two bytes-per-row figures:** 1,115 B for deliveries and ~400 B for audit. **The 1,115 is one number, not two** — `BYTES_PER_ROW` at `webhooks/retention.ts:61`, and the document says so in its own words (*"the same figure §2 and §3 use, deliberately not a second one"*), which is the clause this ticket shares with PF-797 and the one most likely to drift. **Two monthly storage numbers**, per tier, in §2's *Delivery log @30d* and *Audit raw @30d* columns; hand-checked at the 100-user tier as `5,000 × 30 × 1,115` = 167.25 MB (healthy) → `× 6` = 1.0035 GB (worst) and `20,000 × 30 × 400` = 240 MB. **A reason per window, and both are constraints rather than preferences.** Delivery log: long enough to debug a subscriber outage across a holiday weekend — replay usefulness, with the DLQ tail handled separately by `DLQ_RETAINED_INDEFINITELY` (`webhooks/retention.ts:88`) so an unreplayed dead letter is never aged out. Audit raw: p.13 grades Epic 7 on the agent's audit rows, so a window that deletes the evidence before grading is the wrong window at any price — the agent-went-through-the-front-door proof the criterion names. The rollup is indefinite (`audit/retention.ts:54`) and what is deliberately lost at 30 days is stated rather than glossed: per-app-per-day counts survive, per-route detail does not. **One weaker figure, recorded rather than hidden:** the audit's **~400 B** is an estimate carrying a tilde and no source line, unlike the delivery log's cited constant — p.10 asks for bytes per row and does not require it be a code constant, and the 240 MB / 240 GB column moves with it. Re-runnable: `grep -n "RETENTION_DAYS\|BYTES_PER_ROW\|DLQ_RETAINED" api/src/platform/webhooks/retention.ts api/src/platform/audit/retention.ts`. | SUB:AI Cost Analysis | p.10 | PF-797, PF-798 |
| PF-802 | ☑ Close the baseline handoff — L01 captures it, L26 consumes it | L01 PF-020 writes P95 latency, bundle size and per-route query counts to `docs/baseline-part1.json`; this lane's comparator reads it. Closes when the file's schema is documented in one place both sides cite, and the comparator **fails loudly** on a missing, empty, or schema-mismatched baseline rather than passing vacuously. L01's audit notes flagged this exact handoff as a possible gap between lanes — a comparator that green-lights an absent denominator is what that gap looks like in production | MVP-9 | p.2, p.6 | PF-020 |
| PF-803 | ☑ Comparator computes all three metrics as explicit deltas | For each of P95 latency, bundle size, and per-route query counts the comparator emits current value, baseline value, and percentage delta. Query counts are reported **per route**, not aggregated — p.2's wording is *"per-route query counts"*, and an aggregate hides a single route that tripled. Output is a committed artifact a grader can read, not console text | MVP-9 | p.2, p.6 | PF-802 |
| PF-804 | ☑ CI job fails the PR above +10% on any of the three metrics | Proven by seeding a deliberate ~11% regression in each metric in turn and showing the job fails three separate times, each with a message naming the metric, the affected route where applicable, and both numbers. p.18's Pre-Search question asks whether enforcement is manual, a baseline comparison, or a failing perf job — this ticket is the answer being *a failing perf job*, and a job that has never been seen to fail is not evidence of one | MVP-9 | p.2, p.6, p.18 | PF-803 |
| PF-805 | ☑ Record MVP item 9 evidence — both halves | The gate item is two claims. First: the existing Playwright regression suite passes **on main**, recorded with the run date and link (run via `/e2e-test-runner`, never `pnpm test:e2e` directly). Second: the three deltas from PF-803, each at or under +10%. Both pasted into `SUBMISSION-PLUGFORGE.md` under the MVP row. A green suite with no delta numbers satisfies half a gate item **Verified 2026-08-13:** Bundle 0.00% and per-route query counts 0.00% recorded; latency now enforced and within budget after the baseline re-capture. Playwright regression re-run on the final tree is the remaining half. | MVP-9 | p.2, p.6 | PF-019, PF-804 |
| PF-806 | ☐ Demo video — the five-line story, then the dev-portal replay | Runtime between 3:00 and 5:00. The five-line story (p.6) runs live in order in a fresh terminal: `pnpm install @ship/sdk` → `ship login` → `ship docs create` → `ship webhooks tail` → a `document.created` delivery arrives with its signature verified. Then the video switches to the dev portal and replays one delivery, per p.12. Ticket carries a shot list mapping all six beats to timestamps, so "the demo shows it" is checkable rather than asserted | SUB:Demo Video | p.12, p.6 | — |
| PF-807 | ☑ Per-Epic Write-up — all four headings, every epic | `docs/per-epic-writeup.md` has one entry per epic, each with **before → fix → after → proof** as four non-empty headings in that order. An entry whose `proof` is a description rather than an artifact fails the check. p.13 names the shape; a write-up that narrates the work without proving it is the common failure and the reason `proof` is a separate heading rather than a closing sentence **Done 2026-08-15.** `docs/per-epic-writeup.md`, seven epics, `grep -c '#### Before/Fix/After/Proof'` → **7 · 7 · 7 · 7**, each in that order. Epic numbering derived from p.11's Priority Order and the derivation is stated in the document — the PRD fixes only `(E6)` and `(Epic 7)`, both grep-verified to `page-11.txt`. Every Proof is a pasted command + output: E1 `oauth/{endToEnd,authCodeGrant,deviceGrant,deviceScenario,rotation,bearer,mvpGate3}.test.ts` + `apps/secrets` + `scopes` → **140/140, exit 0**; E2 `scripts/check-boundary-lint.mjs` → **11 fences verified, exit 0** plus 203/204 (the 1 is F200's doc latch, named); E3 `src/platform/openapi` → **206 tests, 8 files, exit 0**; E4 `webhooks/{testingScenario6,testingScenario7and8,signer,signatureVectors,dlqAndReplay,retryScheduler,bus,publishFitness}` → **156/156, exit 0**; E5 `pnpm --filter @ship/sdk test` → **282 passed / 5 failed**, all 5 F200 doc latches; E6 + E7 below. | SUB:Per-Epic Write-up | p.13 | PF-781 |
| PF-808 | ◐ Epic 6 proof — the TTFE drill passing **in CI** | The Epic 6 entry links a specific CI run in which `pnpm drill ttfe` passed, and quotes that run's elapsed time against the < 60 s target. p.13 says *"proof is the TTFE drill passing in CI"* — a local run, a screenshot of a terminal, or a green badge with no run ID does not satisfy the word *CI*. The stage timings from the drill's own instrumentation appear alongside the total **LEFT ◐ 2026-08-15 — there is no such run and the ticket must not be closed on a local one.** Every `ttfe` job on `joshrochon/ship` has failed; zero passes, across `main`, `pf/integration` and every slice branch. **Enumeration count updated by S3a:** this row originally said *"30+ runs enumerated over `projects/…/jobs` pages 1–10"*; `docs/ai-cost-analysis-plugforge.md` §1 has since read **all 72** completed `ttfe`/`ttfe-controls` jobs in the project's history and refines the failure mode — **70 of the 72** exit 127 at `docker pull postgres:16`, **not every one of them**. The two exceptions are jobs **65807** and **65808** (pipeline 20073), which died earlier still, pulling `node:22-bookworm` against a Docker Hub **429 Too Many Requests** — which is why they are 2.5 s and 2.8 s rather than ~28 s. Neither exception softens this ticket: a job that dies at image pull ran *less* of the drill, and the conclusion is unchanged — **no job in the project's history has ever succeeded.** The two most recent are pipeline **20166** jobs **66185** (`ttfe`) and **66186** (`ttfe-controls`), both **exit 127** on `$ docker pull postgres:16` → `/usr/bin/bash: line 196: docker: command not found`. The drill's own gate passes first (`ttfe fitness OK — 15 file(s)`), so the failure is the runner, not the drill: the image carries no Docker binary and these two jobs declare no `dind` service — deliberately, per the comment above the job, because `docker:27-dind` never attached to this runner's job network. **Unblocks on a Docker-enabled executor or a mounted socket; it is infrastructure, not code.** Recorded in the write-up with the trace, and the local run (stages + **6 505 ms** total vs the 60 000 ms budget, `loadRatio` 0.634 certified, `check-series.mjs` OK) is labelled as local. | SUB:Per-Epic Write-up | p.13, p.8, p.6 | PF-807 |
| PF-809 | ☑ Epic 7 proof — the agent's audit-log rows showing OAuth app authentication | The Epic 7 entry embeds real rows from a flag-on run — timestamp, the agent app's `client_id`, route, scope, status — **and** the query that produced them, so a grader can re-run it against the deployed instance. p.13 is specific that the proof is the rows. A row set whose `client_id` is null or is the primary account disproves the claim rather than proving it **Done 2026-08-15.** Live capture, not a fixture: `ship_wf_l26` migrated with `AGENT_CLIENT_SECRET` set (`✅ 1 first-party app(s) seeded`), `api/src/index.ts` booted on `:3131`, token minted over the wire with `grant_type=client_credentials` (no `refresh_token` in the response, per RFC 6749 §4.4.3), four reads + one deliberate write, then `docs/l23-epic7-proof.sql` run verbatim. **5 rows**, every one `client_id = ship_app_firstparty_fleetgraph_agent`, every `user_id` NULL, `scope_used` = the granted read scope and NULL only on `/me` (F146's clause, honoured), `status` 200×4 and **403 on `POST /api/v1/documents`** — the platform enforcing read-only, in the trail. Counter-checks: `distinct_users = 0`; `SELECT DISTINCT user_id … WHERE user_id IS NOT NULL` → **0 rows**. The half rows cannot prove is the fitness test: `agentCitizenFitness.test.ts` + `agentAppCitizen.test.ts` + `clientCredentials.test.ts` → **50 tests, 3 files, exit 0** locally — with the honest caveat that `agentCitizenFitness` failed in CI job **66027** on the not-zero-rows assertion and passed in **66183** (flaky, filed F201). | SUB:Per-Epic Write-up | p.13 | PF-807 |
| PF-810 | ☑ Three Discoveries — capture as they happen, select at the end | A dated running log, one entry per discovery written the day it happened, each stating what was expected, what was actually found, and the commit or artifact that proves it. At Final exactly three are promoted to `docs/three-discoveries.md`. p.13 names strong candidates (Device Grant in TypeScript, Zod-driven OpenAPI generation with fitness-test parity, Stripe-style HMAC + timestamp anti-replay, async-iterator pagination as a DX pattern) — a promoted entry that restates a candidate without our own specific finding and its evidence fails. Discoveries reconstructed on Sunday from memory are indistinguishable from the PRD's list, which is why the log is the ticket and the selection is the footnote **Done 2026-08-14.** `docs/three-discoveries.md`. Three promoted entries, none of them a restatement of a p.13 candidate — the candidates turned out to teach the spec rather than anything about this system, and the doc says so in its opening. Each entry states what was expected, what was found, and the artifact: F100's `is_public` column honoured by the guard and written by nothing (`60fe2ba`, plus the `ON CONFLICT DO UPDATE` branch that would have kept the stale value on every environment with history); the regression harness timing its own server binds, caught by `/health` — a route with no query — reporting +32% and once +108% (`docs/regression-paired-runs.md`, `scripts/perf-self-check.mjs`); and the Device Grant's real difficulty being the tenancy binding at consent rather than the polling contract (`deviceVerify.ts:310-322`). Every file, line and commit reference re-opened and verified. | SUB:Three Discoveries | p.13 | — |
| PF-811 | ☐ Social post tagging @GauntletAI with the required screenshot | Post tags @GauntletAI and its screenshot is the `ship webhooks tail` terminal showing a verified signed event arriving in real time — p.13 specifies that terminal, so a portal screenshot, a code screenshot, or a staged mock does not satisfy the row. Post URL recorded in `SUBMISSION-PLUGFORGE.md` and resolving publicly | SUB:Social Post | p.13, p.6 | PF-781 |
| PF-812 | ☐ Pre-Search Document — submission-readiness check only | L25 authors it; this ticket reads p.13's row literally and confirms both halves from a **clean clone**: all three phases present with written answers and no placeholder text, and the saved AI conversation attached as a reference artifact at a path that resolves. Ticket does not review the answers — L25 PF-772/PF-776 own completeness. It fails only on the two conditions p.13 names | SUB:Pre-Search Document | p.13 | PF-771, PF-776 |
| PF-813 | ☐ OpenAPI Spec — the submission artifact, both copies, validated | L13 generates; this ticket ships the row. Confirms from outside the repo and outside localhost: the live `/api/v1/openapi.json` on the deployed instance returns 200 without credentials, `docs/openapi.json` exists in the repo and is byte-identical to the served document, and the served bytes validate against the OpenAPI 3.1 schema. Both URLs recorded in `SUBMISSION-PLUGFORGE.md`. p.13 requires the live URL *and* the static copy — shipping one is half the row **Re-verified 2026-08-15, and the missing piece — schema validation — is now actually run.** Live, uncredentialed, from outside the repo: `https://d258p92d3n1ebe.cloudfront.net/api/v1/openapi.json` → **200 `application/json`** on the first try, no fallback host needed. **`openapi: 3.1.0`, 14 paths**, and the 14 keys are **set-equal** to `docs/openapi.json`, which is also `3.1.0` with 14 paths. `json.load(live) == json.load(committed)` → **True** over the whole document, not just `paths`; top-level keys `components, info, openapi, paths, servers, webhooks` and `info` identical. **Validated against the OpenAPI 3.1 schema for the first time:** `npx --yes @redocly/cli@latest lint live-spec.json` → **exit 0**, *"Your API description is valid"*, **0 errors**, 3 warnings (`info-license`; `no-ambiguous-paths` on `/webhooks/deliveries/{id}` vs `/webhooks/{id}/rotate`; `operation-4xx-response` on `GET /openapi.json`). That closes the one clause of p.13 and of PRD Testing Scenario 5 (p.5) that had never been executed. **Still ◐ on one clause, and it is ours rather than the PRD's:** not byte-identical — **63 492 served vs 147 908 committed** (`cmp` differs at char 2), because the server minifies through `res.json` and the repo copy is pretty-printed. p.13 asks for a live URL *and* a static copy; it does not ask for byte equality, so **this needs a decision: correct the criterion to parse-and-compare (which passes), or commit a minified `docs/openapi.json`** — the latter touches L13's generated artifact and is not this lane's to change. See L99 F163 and `SUBMISSION-PLUGFORGE.md` §5. | SUB:OpenAPI Spec | p.13 | PF-368, PF-369, PF-372, PF-629 |
| PF-814 | ◐ Deployed Application — README credentials and a reachability check from outside | L21 deploys; this ticket verifies the row as a grader experiences it. From a machine with no project state: the public URL loads, the dev portal is reachable, the OpenAPI spec resolves, and the README section carries the grader `client_id`, its read-only scopes, and the one-command CLI setup — run verbatim from the README and observed to work. p.13 requires *"credentials in the README"* and *"Dev portal reachable; OpenAPI spec resolvable"*; a credential that only works from the author's shell is not a credential in the README **Re-checked from outside 2026-08-15. Three of four surfaces pass; the credential clause still fails, and it now fails for a reason with a price attached.** Reachability, curl with no credentials or project state: `https://d258p92d3n1ebe.cloudfront.net/` **200 text/html**; `/portal` **200** (route registered at `web/src/main.tsx:325`, plus `portal/:appId` at `:326`); `/api/v1/openapi.json` **200 application/json**. **Root and `/portal` return byte-identical 4 798-byte SPA shells** — correct for a client-routed SPA, and it means curl learned nothing about whether the portal works. **Stated rather than glossed: `curl` cannot see CSP violations, JS errors, or failed asset loads, so three 200s are not proof the app renders. This ticket does not claim it renders** — the only in-repo assertion that the deployment is up is `docs/infra/grader-access.md` §6, and confirming render needs a browser. **README (`README.md`, section from line 35):** both grader `client_id` values are literal and published — `ship_app_grader_readonly` (`:87`) with `documents:read, issues:read, sprints:read`, and `ship_app_grader_demo` (`:88`) — so the *read-only scopes* clause passes. **The `client_secret` values are still not in the README** (`:98-106` gives `aws ssm get-parameter --name /ship/dev/GRADER_CLIENT_SECRET --with-decryption` instead), and a grader has no credentials for account `379484935796`, so for them the command is not a credential. p.13 says *"credentials in the README"* and `terraform/platform-apps.tf`'s own header already concedes the point — *"the README has to publish the grader's secret (p.13), so a value nobody can read back would defeat the deliverable"* — which is why the parameters are `random_password` + SSM rather than show-once. **The secret is readable** (confirmed this run with the repo's own AWS identity, `arn:…:user/ship-terraform`), so the blocker is not access. **Publishing a live credential into a repo that is public on GitHub is a decision with a real trade, and it is the user's — not made here.** **One thing this run did fix.** The README still carried a `/oauth/*` warning saying the path did not route through CloudFront and that `ship login` could not complete there, and it sent graders to the plain-HTTP EB origin for OAuth — contradicting `SUBMISSION-PLUGFORGE.md` §9, which recorded the CloudFront behaviour as shipped. Re-measured from outside rather than picking a side: `POST https://d258p92d3n1ebe.cloudfront.net/oauth/device/code` → **200 `application/json`**, `GET …/oauth/device/verify` → **302 with `server: nginx`** — the API answering, not the S3 SPA shell. The README was the stale half; corrected, and the EB-origin workaround block replaced with the single-host commands. So the *one-command CLI setup* clause now points at commands that work, and the remaining shortfall on this row is the `client_secret` decision alone. | SUB:Deployed Application | p.13, p.18 | PF-628, PF-630, PF-631 |
| PF-815 | ☐ Final assembly — ten rows Ready, deadline resolved, submitted | `SUBMISSION-PLUGFORGE.md` shows all ten Submission Requirements rows Ready with a resolving path or URL each; PF-782's confirmed deadline is recorded and the submission happens before it; a clean clone plus the recorded URLs reproduces every deliverable with no reference to the author's working tree. Last check before submitting: **no merged-branch pruning has occurred** — `repo-cleanup`, `git branch -d`, and auto-delete-head-branch each destroy graded evidence that the remote cannot return once both sides are gone | — | p.12, p.13 | PF-781, PF-783, PF-784, PF-785, PF-793, PF-794, PF-805, PF-806, PF-807, PF-810, PF-811, PF-812, PF-813, PF-814 |

## S4 — landed 2026-08-13 (`pf/L26-regression-budget`)

Full write-up with every number and exit code: **`docs/mvp-gate-item-9.md`**.

| Ticket | Evidence |
|---|---|
| PF-802 ☑ | Schema documented once in `docs/baseline-schema.md`, cited by producer, consumer and both CI jobs. Comparator throws `BaselineError` and exits 1 on missing / empty / non-JSON / non-object / each missing top-level key / absent budget / zero bundle figure / zero routes / route missing `latencyMs` or `queriesPerRequest` / non-positive P95 / negative query count / a baselined route absent from the run. Observed live: missing → exit 1, empty → exit 1. Anti-vacuity: the real committed baseline loads and is enforceable, and the control run exits 0. |
| PF-803 ☑ | `pnpm baseline:compare` emits current, baseline and % delta for all three metrics; query counts **per route**, never aggregated (a test seeds the hiding case: documents 3→9, my-work 7→1, total unchanged at 10 — the per-route failure still fires). Committed artifacts `docs/regression-report.md` + `.json`, uploaded by CI every run. |
| PF-804 ☑ | Job `regression-budget` in `.gitlab-ci.yml` (authoritative) and `.github/workflows/ci.yml`. Four real CLI runs: control **exit 0**; P95 `GET /api/documents` +11.01% **exit 1**; bundle +11.00% **exit 1**; queries `GET /api/dashboard/my-work` 7→8 +16.67% **exit 1**. Each message names metric, route where applicable, and both numbers. Re-runnable: `pnpm baseline:evidence`. |
| PF-805 ◐ | **Bundle −0.00% and query counts 0.00% on all six routes — both PASS, recorded.** (b) **CLOSED 2026-08-14 by L26:** the Playwright suite now passes on the **integration** tree — **881 passed, 1 flaky, 0 failed, exit 0** at commit `c728c40`, 4 workers, 10.5 m. Getting there took two fixes, neither in the platform: a stale `404 / not_found` assertion in `oauth-pkce.spec.ts` that its own comment had predicted would become 200 once L09's PF-245 landed, and — revealed only once that stopped skipping four tests under `mode: 'serial'` — the PKCE P95 measurement 429ing because 20 iterations × 2 `/oauth/*` requests exceeds the 30/min IP-keyed throttle (L99 F165). Full run table in `SUBMISSION-PLUGFORGE.md` §11. (a) **P95 still not certified** — the machine was at load ratio 1.33–1.88 and three runs of one commit spread up to 6.0×, ~60× the budget (F80); needs one run of `pnpm baseline:compare -- --strict-latency` on an idle box, and this session's four back-to-back suite runs make now the worst possible time to take it. |

Fitness test for the slice: `api/src/scripts/lib/perf-compare.test.ts` — **36 tests, passing**. Full
api suite after the change: **146 files, 2507 tests, passing**.

## S1a — landed 2026-08-14 (`pf/L26-submission-truth`)

The submission index and the deployment-URL correction. Not all of S1: PF-784 and PF-785
(the PR-body sweep and the slice↔PR bijection) are untouched and stay open.

| Ticket | State | Evidence |
|---|---|---|
| PF-781 ☑ | done | `SUBMISSION-PLUGFORGE.md` — ten rows named verbatim from p.12–p.13, each with owning lane, resolving path/URL, and Ready / Not-ready. Counted honestly: **1 Ready, 1 Ready-with-caveat, 8 open.** Every Not-ready row carries the specific condition that would close it. |
| PF-782 ◐ | **open, and deliberately** | The contradiction (p.1 `11:59 AM CT` vs p.12 `Deadline: Sunday 11:59 PM CT`) is recorded in the index with an empty asked/channel/answer block, and the board plans against the **earlier** time until a grader answers. Closing this by picking a page is the assumption the ticket exists to prevent. |
| PF-783 ◐ | half measured, and it fails | (a) Public: `github.com/joshdrochon/ship` → **200** logged-out ✅; `labs.gauntletai.com/joshrochon/ship` → **302** to sign-in (L99 F164). (b) Branches preserved: **119 local `pf/*`, 11 on GitLab, 5 on GitHub — 108 never pushed** (L99 F161). The criterion asks for a count match against declared slices; it fails long before that comparison. |
| PF-786 ◐ | measured, and it fails on length | All **nine** required headings present and correctly named in `docs/architecture.md`. The p.13 / p.11 cap is *1–2 pages*; the file is **744 source lines** with five mermaid diagrams and thirteen subsections beyond the nine. Measured as source lines because no rendered artifact exists to page-count — either way 744 lines is not two pages. The fix is a cut, not an addition. |
| PF-812 ☑ | done | `PRESEARCH-PLUGFORGE.md` — all three phases with written answers, no placeholder text (the sole `TODO` occurrences are citations of code comments, which the document itself calls out); reference artifact `docs/presearch-conversation.md` present and linked both ways with repository-relative paths. |
| PF-813 ◐ | live half proven, criterion needs correcting | Live spec **200 uncredentialed** from outside the repo on both hosts; **14 paths, set-equal** to `docs/openapi.json`; **semantically identical**; `3.1.0`. **Not byte-identical** (63,436 served vs 147,852 committed — the server compacts, the repo copy is pretty-printed). See L99 F163. |
| PF-814 ◐ | **fails, and the reason is new** | Public URL loads, `/portal` reachable, spec resolvable, README carries both grader `client_id`s and their scopes. But `/oauth/*` is not routed through CloudFront, so `ship login` cannot complete on the Week 6 URL and the API's own `verification_uri` points into the same hole — **L99 F160**. Separately: the README gives `client_secret` via an `aws ssm get-parameter` command rather than a value, and p.13 says *"credentials in the README"* — flagged, not silently counted as satisfied. |

**PF-794 had not been started as of this slice, and the file that looked like it was last week's.**
`docs/ai-cost-analysis.md` exists and opens by citing the **Week 5** brief. It contains none
of p.13's three halves — no tracked dev spend for Epic 7, no production projections table, no
fanout / active-rate / retention assumptions. This is precisely the failure mode this lane's
own header warns about: a file with the right name in the right directory is not a
deliverable. PF-794–801 were fully open at S1a.

> **Superseded by S3a (2026-08-15).** The deliverable shipped as
> `docs/ai-cost-analysis-plugforge.md`, a **separate** file — Week 5's `docs/ai-cost-analysis.md`
> is left as-is on purpose. PF-794, PF-797 and PF-801 are now closed; PF-795, PF-796, PF-798,
> PF-799 and PF-800 are ◐ with the specific remaining clause named on each row. The
> paragraph above is kept as the S1a record rather than deleted.

**Documentation corrected in this slice.** `README.md` now leads graders at
`https://d258p92d3n1ebe.cloudfront.net/` for everything, states plainly why the EB origin
white-screens in a browser, and carries the `/oauth/*` warning with the one command that
needs the EB host. `SUBMISSION.md` and `CREDENTIALS.md` get a Week-5 banner pointing at
`SUBMISSION-PLUGFORGE.md` — **their Render URLs are not corrected**, because they describe
Week 5's application, which is still live, and rewriting them would falsify a record the
repo's own `scripts/check-doc-links.sh` exists to protect.

**Two files deliberately left alone.** `docs/infra/topology.md` and
`docs/infra/grader-access.md` were reported as pointing at the wrong deployment; re-measured,
neither does. topology.md §4 names the **retired** `shipshape-7buc` as a documented dead
fallback with the `x-render-routing: no-server` 404 pasted in, and grader-access.md §6
already records that `eba-xsaqsg9h` is dead and that `eba-nvpntpge` answers. Both are correct
as written. `.claude/CLAUDE.md` **does** still name the dead `eba-xsaqsg9h` CNAME and was not
edited here — it is project configuration, and the README now says so explicitly.

## S2a — reconciliation and as-built sweep, 2026-08-15

Verification pass over the rewritten `docs/architecture.md` (859 → 149 lines, depth moved to
`docs/architecture-appendix.md`). Every claim re-derived from code in this pass; nothing
carried over on trust. Full as-built table: **`SUBMISSION-PLUGFORGE.md` §4b**.

| Ticket | State | One-line evidence |
|---|---|---|
| PF-788 ☑ | closed | G1 was never a defect. `api/src/db/migrate.ts:125` calls `seedPlatformApps`; the doc says `db:migrate`, not a numbered migration, and `appendix:761-769` explains why the distinction is load-bearing. |
| PF-789 ☑ | closed | Doc list (`architecture.md:20`), shipped column (`057_public_api_calls.sql:46`, `NOT NULL`) and p.18's list all carry `request_id`. Doc omits the shipped `method` column; recorded, non-blocking. |
| PF-790 ◐ | half | Env-group defect gone (zero `render_env_group` in `terraform/`); Deployment Topology is AWS and diffs clean. **But** `appendix:871`'s zero-new-resources claim is unsourced in the PRD and contradicted by six PlugForge-only resources in `terraform/platform-apps.tf`. Routed to L21. |
| PF-791 ☑ | closed | Agent-as-Citizen names Client Credentials, cites RFC 6749 §4.4, and names both rejected alternatives with the reason — `architecture.md:126-128`. |
| PF-792 ◐ | half | F8 resolved and its premise stale: both surfaces really do call `api/src/services/documents.ts`. F11 **still wrong** — the Composition Root sketch (`appendix:81-83`) mounts the spec after `/api/v1`; `platform/openapi/route.ts:9-11` says so in its own header, and the shipped mount is `app.ts:386`. |
| PF-793 ◐ | swept, not resolved | 16 values checked; **12 match, 4 diverge**. Worst: `appendix:765` gives the agent `issues:write` while the code seeds read-only — our own two documents disagree about the Epic 7 claim. Also cursor envelope (3 keys, not 2), stale SDK footprint (208.8 KiB, not 160.4 KB), demo-app scopes. Fixes all land in files this lane did not own this pass. |
| PF-783 ◐ | re-measured, and it splits | 147 local = **147 on `origin`**, set-difference empty both ways — supersedes 119/11/5 and 127/127. But `origin` 302s to sign-in and the public GitHub remote has **0** `pf/*`. p.12's two words are met by two remotes and by neither alone. |
| PF-785 ◐ | measured, not bijective | 135 unique declared slices · 112 with a branch · 23 with none · 35 orphans. `L00` resolves to no lane file. Some orphans are renames, not misses; separate them first. |
| PF-813 ◐ | validated for the first time | Live spec **200** uncredentialed, `3.1.0`, 14 paths set-equal and `json.load`-equal to `docs/openapi.json`. **`@redocly/cli lint` → exit 0, 0 errors** — closes p.5 Testing Scenario 5's validation half. Only the byte-identity clause fails, and that clause is ours, not p.13's. |
| PF-814 ◐ | three of four surfaces | `/`, `/portal`, `/api/v1/openapi.json` all 200 from outside; `/oauth/*` confirmed routing through CloudFront (device-code **200**, verify **302 `server: nginx`**). **README fixed in this pass** — it still warned that `/oauth/*` did not route and sent graders to the EB origin. Remaining shortfall is the `client_secret`, which is a decision, not a task. |

**Two things this pass did not do, deliberately.** It did not edit `docs/architecture.md` or
`docs/architecture-appendix.md` — another agent held them — so every divergence above is
named and routed rather than corrected. And it did not publish a grader `client_secret`:
the value is readable and the Terraform was built to allow it, but putting a live credential
into a repo that is public on GitHub is the user's call.

## S3a — cost-analysis ticket reconciliation, 2026-08-15 (`pf/L26-cost-ticket-reconcile`)

Board-only pass over PF-794–801 after `docs/ai-cost-analysis-plugforge.md` went through a build,
an independent audit and a fix pass. **No document was edited in this slice** — the cost analysis
belongs to `pf/L26-cost-measurement-fixes` and had already merged to `pf/integration` at `b5c6bf2`.
Every claim below was re-derived from code or from the GitLab API in this pass; nothing was
carried on trust from the document's own prose.

| Ticket | State | One-line evidence |
|---|---|---|
| PF-794 ☑ | **evidence corrected** | The row asserted *"Marginal AI spend $0.00, measured"* while the document it produced retracts exactly that. `client.ts:181` selects the direct Anthropic API and `:249` builds `ChatAnthropic`, so Cost Explorer cannot see the spend. Production $0.00 stands on the env-var check; development is an explicit, unmeasured gap. Scaffolding criterion genuinely met — **not** reopened. |
| PF-795 ◐ | gap stated, delta absent | Production $0.00 and the dev gap bounded (1 call/turn, `MAX_TOKENS` 2,048, scans **unrecorded**). No per-day series, and the flag-off/flag-on token delta the criterion demands does not exist — the doc argues it by construction (`grep -rn SHIP_AGENT_VIA_SDK agent/src/llm/` → **0 hits**). |
| PF-796 ◐ | three numbers, two short a clause | Day-1 = pipeline **19893**, re-verified live: jobs 65064 `ttfe` **69.87 s** + 65065 `ttfe-controls` **32.90 s**. Weekly **59.4 h** summed over 62 pipelines; no dollars, correctly, because runner 198 is `is_shared: false`. Missing: OAuth count → compute minutes (refused, soundly) and `openapi-freshness`'s share of the CI bill (simply not computed). |
| PF-797 ☑ | closed | 3 rows/run × **1,115 B** (`webhooks/retention.ts:61`) = 3,345 B, 20,070 B worst. Weekly accrual **structurally zero** — `harness.ts:149` drops the database, `:154` re-queries `pg_database` and throws on a survivor, `:165–169` disposes the container. Egress re-derivable: **633 / 886 / 15,879 B**. |
| PF-798 ◐ | structure yes, labels no | Tiers reproduced, constants cited, arithmetic hand-checked (167.25 MB / 1.0035 GB / 240 MB), disagreement with p.9 shown side by side. No cell carries `[PRD]` or `[derived]`; provenance is column-level prose. |
| PF-799 ◐ | ratio stated, denominator wrong | 0.25 is deliveries ÷ **API calls**, not ÷ writes, and the criterion says per write operation. Subscriptions-per-event-type never given a value. No reconciliation recorded against `PRESEARCH-PLUGFORGE.md:82`'s `6N`-per-event form. |
| PF-800 ◐ | self-contradictory, sensitivity missing | *"~1 turn per active user"* against its own parenthetical *"~10 turns each"* — the parenthetical's arithmetic is right, the bold figure is 10× off. The 2× sensitivity p.10 demands is absent (10% → ~$6,000–12,000/month). |
| PF-801 ☑ | closed | Two windows **30 / 30** (`webhooks/retention.ts:76`, `audit/retention.ts:51`), two bytes figures with 1,115 shared with PF-797 as one number, two monthly storage columns, and a constraint-shaped reason each. Audit's **~400 B** is an estimate with no source line — recorded, not hidden. |

**Two document defects found and routed, not fixed here.** PF-800's ~1-vs-~10 turns contradiction
and PF-799's write-vs-API-call denominator both live in `docs/ai-cost-analysis-plugforge.md`, which
this slice does not own. Both are one-line corrections. The turns contradiction is the more urgent
of the two: it is visible in a single sentence and it is the number p.10 says the whole projection
bends on.

## Slices

One branch and one PR per slice, per PRD p.12. Branch name is `pf/L26-<slug>`; the PR body names
the acceptance criterion each slice advances and confirms its fitness test passed. Most of this
lane's fitness tests are completeness checks over an artifact rather than a test run — stated as
such in the PR body rather than dressed up as a green suite. S4 is the exception: it ships a real
CI job and its fitness test is that job failing on a seeded regression.

| Slice | Branch | Tickets | Advances | Fitness test |
|---|---|---|---|---|
| S1 | `pf/L26-repo-and-pr-discipline` | PF-781–785 | Submission index exists; the GitHub Repository row is verifiably satisfied (public, branches preserved, PR bodies compliant) | Index has ten rows matching the PRD table; `git ls-remote` returns a branch per declared slice; zero non-compliant merged PRs; slice↔PR mapping bijective |
| S2 | `pf/L26-architecture-doc` | PF-786–793 | Architecture Document row complete — nine sections verified against the p.12 contract and reconciled against the code | Nine-heading check passes; nine per-section rows each cite a line; G1–G4 each closed in one recorded direction; as-built value table has zero unresolved divergences |
| S3 | `pf/L26-cost-analysis` | PF-794–801 | AI Cost Analysis row complete — tracked dev spend, projections table, and all three named assumptions | Every dev-cost bullet carries a number; every projection cell labeled `[PRD]` or `[derived]` with arithmetic; fanout ratio, agent active rate, and both retention windows each state a number and its reason |
| S4 | `pf/L26-regression-budget` | PF-802–805 | MVP gate item 9 — regression suite green and P95, bundle size, per-route query counts within +10% of the Part 1 baseline | Comparator fails on an absent baseline; seeded ~11% regressions fail the CI job three separate ways with named metrics |
| S5 | `pf/L26-demo-and-writeups` | PF-806–811 | Demo Video, Per-Epic Write-up, Three Discoveries, Social Post | Video 3:00–5:00 with six timestamped beats; every epic entry has four non-empty headings; Epic 6 links a CI run ID and Epic 7 embeds rows plus the query; exactly three discoveries, each with its own evidence; post URL resolves and tags @GauntletAI |
| S6 | `pf/L26-final-assembly` | PF-812–815 | The three rows other lanes produce are submission-ready, and the submission goes out | Clean-clone reproduction of every deliverable; live spec 200s uncredentialed and matches `docs/openapi.json`; README one-command setup run verbatim; ten Ready rows before the confirmed deadline |

## Notes for the audit agent

Read the full PRD, not just the pages cited above. Known thin spots in this lane, stated so you
can confirm or refute rather than rediscover.

**Which page the Submission Requirements table is on.** Both p.12 and p.13, and the split is not
cosmetic. p.12 carries the heading, `Deadline: Sunday 11:59 PM CT`, and the GitHub Repository and
Demo Video rows. p.13 carries the other eight. The Required Documentation nine-section table is
also p.12 — its heading's last line (*"Architecture Document (1–2 pages) committed at
docs/architecture.md"*) is the final line of p.11. So a ticket about branch discipline cites
p.12, a ticket about the cost analysis cites p.13, and both are right. Do not normalize them.

**Submission Requirements coverage: 10 of 10 rows.** GitHub Repository → PF-783–785 · Demo Video
→ PF-806 · Pre-Search → PF-812 · Architecture Document → PF-786–793 · OpenAPI Spec → PF-813 ·
AI Cost Analysis → PF-794–801 · Per-Epic Write-up → PF-807–809 · Three Discoveries → PF-810 ·
Deployed Application → PF-814 · Social Post → PF-811. If you find a row that isn't reachable
from a ticket, PF-781's ten-row index is where it should have been caught.

**No `CTR:` entries in this lane, deliberately.** Everything L26 owns is either a Submission
Requirements row or MVP gate item 9. If you think a Core Technical Requirement (p.2–p.5) landed
here uncited, name it — but check first that it isn't L13's, L20's, or L21's.

**The +10% handoff was a real gap and PF-802 is the whole reason this slice exists.** L01's audit
notes say: *"The +10% regression budget (p.6) is captured here but enforced in L26. Confirm the
handoff is real and not a gap between lanes."* It was a gap: PF-020 writes `docs/baseline-part1.json`
and, before this lane, nothing read it. The failure mode to check for is not a missing job — it
is a job that runs, finds no baseline, and reports success. PF-802's criterion is specifically
that it fails loudly instead.

**Reproduce-or-derive on the projections table is a judgment call I made and you should re-test.**
p.9 hands over a complete four-tier table. Two readings: the deliverable is that table copied
with our assumptions attached, or the deliverable is our own table derived from our own
assumptions. PF-798 does both — reproduce the structure, label every cell, show the arithmetic
for the derived ones, and surface disagreements side by side rather than overwriting. My reason:
p.13's row demands *"explicit assumptions"*, and assumptions that don't generate the numbers
aren't load-bearing; but silently contradicting the PRD's own figures reads as an arithmetic
error, not a finding. If you think one reading clearly wins, say which — this is the single
softest call in the lane.

**Overlap with L25 on the cost assumptions, and the tie-break.** Fanout ratio, agent active rate
and retention windows are Pre-Search answers (L25 PF-753, PF-754) *and* cost-analysis inputs
(PF-799, PF-800, PF-801). L25's own scope note sets the rule: the Pre-Search is the earlier
document and L26 moves if they disagree. PF-799 states that explicitly; PF-800 and PF-801 should
inherit it. Bytes-per-row is shared between PF-797 and PF-801 and must be one number, not two.

**PF-793 and L25 PF-773 run in opposite directions, on purpose.** PF-773 aligns the Pre-Search to
`docs/architecture.md`. PF-793 aligns `docs/architecture.md` to the shipped code. Run PF-793
first if you can — otherwise PF-773 can align the Pre-Search to a doc that is about to move.
That sequencing is not encoded in the Deps column because L25 lands earlier in the week; flag it
if you think it should be.

**G3 belongs to L21, not here.** PF-790 verifies PF-622's landing. If you find PF-790 re-writing
the Deployment Topology paragraph rather than checking it, that is scope creep and should be cut.

**Two tickets I could not make fully mechanical.** PF-782 (the deadline) closes on a human answer
from a grader — there is no check that resolves a document contradicting itself, and asserting PM
because it appears in the Submission Requirements section would be exactly the assumption the
ticket exists to prevent. PF-806 (the demo video) can be checked for runtime and beat coverage
but not for whether the five-line story actually *worked* on camera; that depends on L19 and L20
having landed, and it is the ticket most likely to be discovered broken on Sunday.

**Things this lane deliberately does not claim.** MVP-10 (deployed + public spec URL +
pre-registered grader app) is L21's — PF-814 checks the same surfaces from the grader's side and
advances `SUB:Deployed Application` rather than double-claiming the gate item. Likewise PF-813
advances `SUB:OpenAPI Spec` and leaves MVP-7 to L13. If the audit wants gate items claimed by
every lane that touches them, that is a spine-level decision, not a lane-level one.

**Deps reference only lanes whose files exist** (L01, L03, L07, L08, L13, L14, L21, L25). Real
dependencies on L12 (audit rows for PF-809), L19/L20 (the drill and CLI behind PF-806 and
PF-808), L22 (portal replay in PF-806), and L23 (the rewire behind PF-788, PF-795 and PF-809)
are stated in prose here and must be added to the Deps column once those lane files land.

Cross-lane findings go to `lane-99-unassigned.md`, not into this file.
