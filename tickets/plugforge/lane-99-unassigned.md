# L99 · Unassigned — cross-lane findings

Findings that belong to no single lane, or that a lane author surfaced about
*someone else's* territory. Per the spine's Cross-lane findings rule, auditors file here
rather than into their own lane, so 26 auditors reading the same 18 pages don't produce
26 copies of the same issue.

**ID block:** PF-900+. Nothing here is scheduled until an owner lane adopts it.

## Open decisions — blocking, escalated to the user

| # | Decision | Options | Raised by | Lean |
|---|---|---|---|---|
| ~~D1~~ | ~~`client_secret` hashing~~ **DECIDED 2026-08-12: SHA-256, unsalted.** Consistent with `docs/architecture.md:9` (a graded Final deliverable that already names it) and the existing `api_tokens` precedent at `api/src/middleware/auth.ts:84`. Defense for p.15: the secret is 32 bytes of CSPRNG output, so a salt defends nothing — salts exist to stop precomputation against low-entropy human passwords. Recovery process: none, by design (p.2 "never recoverable thereafter"); a lost secret is rotated, not retrieved. | | user | — |
| ~~D2~~ | ~~OAuth app owner deleted~~ **DECIDED 2026-08-12: deactivate the apps.** `active=false`, tokens stop validating. The only option where a deleted user's access cannot outlive them. Recovery story for p.17: an admin reactivates and reassigns. | | user | — |
| ~~D3~~ | ~~`client_secret` rotation~~ **DECIDED 2026-08-12: instant invalidation.** Old secret dies when the new one is issued. **The p.17 question still has to be answered in the Pre-Search Document** — it asks "what does Stripe do, and why?", and Stripe *does* offer a grace period (0h/1h/24h/3d/7d expiry). L25 must carry the departure and its defense: a one-week build with no production integrations has no migration window to protect, and instant is the safer default. Do not let this land as an unexamined choice. | | user | — |
| ~~D4~~ | ~~Scope upgrades~~ **DECIDED 2026-08-12: re-consent with union.** Requesting a new scope restarts `/oauth/authorize`; the user consents to the full union and a fresh token replaces the old. What a hand-rolled RFC 6749 does by default — no mutable grant records, no partial-grant edge cases. L03's PF-076 already assumed this; clear its `⚑`. p.16's question is answered in the Pre-Search doc as a deliberate cut, not an oversight. | | user | — |
| ~~D5a~~ | ~~Agent OAuth grant type~~ **DECIDED 2026-08-12: Client Credentials (RFC 6749 §4.4).** Defense for p.17: the agent runs on a schedule with no user at the keyboard. Device and Auth Code both require a human browser step the agent cannot perform — using either would mean a human re-authorizes a cron job by hand, or a refresh token is persisted forever while pretending the grant was interactive. | | user | — |
| ~~D5b~~ | ~~Agent scopes: read-only or write?~~ **DECIDED 2026-08-12: read-only + recommendation pattern.** `documents:read`, `issues:read`, `sprints:read`. **This is the decision with the largest behavioral consequence on the board — see B12.** | | user | — |
| ~~D6~~ | ~~Render vs AWS~~ **DECIDED 2026-08-12: migrate to AWS.** Render cannot satisfy the requirements at all, and one of them is on the **hard gate** — p.2 names "IAM task role and execution role" as part of the topology the Terraform config must describe; p.5 adds "VPC/subnets, and security groups" and an `AdministratorAccess`→least-privilege drill that needs a real IAM surface to lock down and a real denial to demonstrate. p.10 permits Render by name, but p.10 is a suggestion table ("Use whatever stack helps you ship") and p.2 is a gate — **a gate outranks a suggestion**. The AWS root already exists (`terraform/elastic-beanstalk.tf`, `database.tf`, 15 `aws_iam_*` resources, S3 backend), so this is `init`/`plan`/`apply` against written code, not a greenfield build. Cost ~$15–25 for the week on existing credits. **Config stays as designed — Aurora Serverless v2 and the NAT gateway are kept.** They are not free-tier eligible; downgrading to `db.t4g.micro` and public subnets would save ~$20 and weaken the blast-radius answer at the Architecture Defense, which is an auto-fail topic. Architecture is not chosen to dodge $20. **Note:** EB uses `aws_iam_instance_profile.eb` + `aws_iam_role.eb_service`, not ECS's task/execution roles — same two-role shape, different names; map it honestly in the write-up. Render config is retained as fallback, not deleted. | | user | — |
| ~~D7~~ | ~~Webhook payload contents~~ **DECIDED 2026-08-12 by L14: full object — the resource's PUBLIC API REPRESENTATION, `documentSchema` itself.** The fact that settles it is one L09 already established and the ticket framing missed: **`content` was never on the table.** `documentSchema` (`platform/api/v1/documents/documents.schema.ts`, PF-252, finding F17) is an allowlist projection and is exactly `{id, document_type, title, parent_id, created_at, updated_at, created_by}` — no `content`, no `properties`, no `yjs_state`. So "ship the full object" and "never ship document bodies" were never opposing positions; the old ids+title middle was choosing between ids-only and a thing that does not exist. Given that: exposure surface does not grow (every field is one the subscriber's own token could already `GET` — the event tells them nothing the API would not), the per-event authenticated `GET` and its availability-coupling disappear, and one `.extend()` means the payload and the SDK's `Document` cannot drift. **F10 forced it regardless** — a hard-deleted document is unfetchable, so ids-only always needed `document.deleted` as a carve-out, and a rule with a mandatory exception on one of its three document events is not a rule. Cost stated honestly: payload size is now proportional to the projection rather than O(1) (bounded — seven scalar fields), and `title` is still user-authored content. **PF-410 answered differently from its ticket:** payloads carry `visibility` rather than suppressing `title`. The bus is in-process and is NOT the exposure boundary — L15's matcher is — and a payload whose shape depends on a row's ACL state is one no consumer can type. `visibility` + the already-present `created_by` let the matcher gate private documents with no DB read, which is the only thing that can work for `document.deleted` where F10 leaves no row to read. **L15 must implement that gate; it is the one piece of D7 this lane could not enforce itself.** | | L14 (PF-408) | — |
| ~~D8~~ | ~~`ITokenStore` scope + refresh threading~~ **CLOSED by L17/L18.** Persist **both** access and refresh — access-only makes `ship login` a device flow per invocation and fails TTFE on the second command. Threading: **single-flight promise**, not naive per-call refresh. p.3 mandates one-time-use refresh with family revocation, so two parallel refreshes present the same token twice and the second **revokes the family**, logging the user out mid-drill. Naive isn't slow, it's destructive. | | L17 PF-504/509 | — |
| ~~D9~~ | ~~4xx-always-permanent?~~ **CLOSED by L16.** Nuanced: 408/425/429 transient, other 4xx permanent. p.4 says "4xx permanent" flat; p.16 asks the question and names "429 transient" — two pages disagree, and L16 took the one that thought about it. Dead-lettering a subscriber for rate-limiting *us* is the one failure a sender cannot have. Reversible in one line plus the table test. | | L16 | — |
| D12 | **The grader cannot run the demo.** The pre-registered app is read-only (p.2, p.13), so `ship docs create` — the headline of the five-line story — fails for anyone following the README. | Pre-register a second write-scoped demo app · document `ship docs ls` as the grader smoke test (what L19 PF-580 does today) · widen the grader app's scopes | L19 | **Second write-scoped app.** The five-line story is the demo *and* the Social Post screenshot; a grader who cannot reproduce it is a graded miss. Owner is L21 or L26 |
| D13 | **The agent's detectors read tables with no public route.** `document_associations` and `document_history` have no `/api/v1` surface and no scope on p.3, yet between them they carry **3 of 5 detectors plus 2 graph fetch nodes**. Verified by L23. | **a.** flatten `sprint_id`/`project_id` onto `issueSchema` — one field, rescues loadImbalance + sprintMissRisk, but is a cross-lane edit to L10's PF-282 · **b.** add `GET /api/v1/issues/:id/history` — rescues reworkChurn, invents a route the PRD never asks for · **c.** leave those reads on SQL, named and counted — cheap, honest, narrows the front-door claim to a bounded one · **d.** disable those detectors flag-on — a regression wearing a flag | L23 (PF-695) | **(a) + (c).** If L10 refuses (a), the exception list grows from one entry to three — still defensible, weaker write-up sentence. (d) is not a real option. |
| D14 | **Single-flight refresh breaks across processes.** L06's server-side rotation is correct — a conditional `UPDATE … WHERE spent_at IS NULL` inside one transaction means ten concurrent exchanges yield exactly one pair and one family revocation. But L17's PF-509 keys its single-flight promise on the **token-store instance**, i.e. one process, while L19's CLI persists to a shared `~/.ship/credentials.json`. Two terminals are two processes sharing one credential — strict rotation revokes the family and logs the user out mid-demo. | **a.** strict; document concurrent CLI as unsupported · **b.** 10s same-generation replay window returning the already-issued pair | L06 (PF-171) | **(b).** Costs nothing against any graded assertion — L24's PF-725 replays a long-spent `R1` after three rotations, far outside any window, and still gets family revocation. Honest counter: p.3 says "reuse invalidates the family" flat, so (b) is a documented departure, same species as D3's Stripe write-up. **L17 update (client side, shipped):** the SDK is built for **(a) strict rotation and assumes no window exists**, so whichever way this lands the client is correct. Single-flight holds within a process; the store is re-read *inside* the critical section so a refresh done by another process is picked up rather than overwritten; a failed refresh is logged-out and never retried. The cross-process case is a lockfile beside `~/.ship/credentials.json` and belongs to **L19**, not to a library that also runs in a browser. |
| D10 | Audit-log retention | 30d raw + indefinite per-day-per-app rollup · raw-only with a window · indefinite raw | L12 (PF-341, p.10) | **30d + rollup** — Epic 7's "the agent went through the front door" must stay provable after raw rows expire |
| D11 | Epic 7 proof mechanism | Audit-log grep · dev-portal dashboard panel · fitness test that runs the agent and inspects the trail | L12 (PF-344, p.18) | **Fitness test** as the graded artifact, SQL query for the demo — the PRD's phrasing is "for every action" and a grep cannot establish that. Cross-lane: L23 owns the test, L22 the panel |

D1–D3 block L02. D4 blocks L04. D5 blocks L23. D6 resequences L21's week.

## Repo defects found while authoring — real, verified, unowned

| # | Finding | Evidence | Suggested owner |
|---|---|---|---|
| ~~F33~~ | **FIXED 2026-08-12** — root `test` script now runs `--workspace-concurrency=1`, so the four packages no longer TRUNCATE each other's data mid-run. Serializing costs wall-clock; a per-package database would keep the parallelism and is the better long-term fix, but it changes every package's `setup.ts` and this was the one-line stop-the-bleeding. ~~Original:~~ `pnpm test` runs packages in parallel against one `DATABASE_URL` while `setup.ts` TRUNCATEs. One L07 run failed 49 tests across `auth`/`fleetgraph`; a re-run passed, `--workspace-concurrency=1` passed, and both files pass in isolation. Pre-existing, but every lane that adds api tests widens the window. **This will bite CI**, and a retry will mask it as flake. Fix is a per-package database. | observed under L07 | unowned |
| F41 | **Every E2E worker ran against a database with NO OAuth tables, and `schema_migrations` said otherwise.** `e2e/fixtures/isolated-env.ts` applies `schema.sql`, then marks EVERY migration file as applied, then actually runs only the ones in a hard-coded list — which was `['038_fleetgraph.sql']`. `schema.sql` stops at 037, so **039–066 were never executed on any worker**: L02's `oauth_apps` + `client_secret_auth_log` + platform-app seed, L06's `oauth_tokens`, L08's keyset indexes, L04's `oauth_authorization_codes`. Step 3's INSERT into `schema_migrations` is what made it silent — nothing would ever re-apply them. The symptom is not "missing table" in a test; it is `POST /api/apps` answering **500**, which reads as "app registration is broken". The 038 note in that file describes this exact failure happening once before; it recurred for the whole PlugForge block. **FIXED 2026-08-12 by L04** as a RULE, not a longer list — everything numbered above `SCHEMA_SQL_COVERS_THROUGH = 37` is applied in filename order, an empty result throws, and a failure to apply names the file and says migrations above the cut-off must be idempotent. Appending filenames is what produced the bug: the list is a second place to remember and the person adding migration NNN is not reading that file. **Consequence for other lanes: any E2E spec touching OAuth, apps or tokens was passing vacuously or not written yet — re-check L02's and L06's e2e coverage against a worker that now has the tables.** | `e2e/fixtures/isolated-env.ts`, migrations 039–066 | found by L04 (PF-108); **fixed** |
| F42 | **`enumerateV1Routes` throws on any app that has a RegExp route, which means any app built where `web/dist` exists.** `joinPaths` does `suffix.startsWith(...)`, and a route registered with a RegExp gives a non-string path — `TypeError: suffix.startsWith is not a function`. `createApp` mounts exactly one such route, the SPA fallback, inside `if (existsSync(webDist))`. So a fitness test calling `enumerateV1Routes(app)` **passes on a machine that has not run `pnpm build:web` and throws on one that has** — including any machine that has just run the E2E suite, since global-setup builds the frontend. That is an environment-dependent failure, which is worse than a real one because the first instinct is to re-run. **Production boot is NOT affected**: `createApp` calls `assertEveryRouteDeclaresList(app, enumerateV1Routes)` before the SPA block, so at that point no RegExp route exists yet. Same family as F37 (the `rootStack` `??` chain), and the same file. L04 worked around it locally by walking the v1 mount's own sub-stack instead; the helper itself is untouched because it is not this lane's. | `api/src/platform/api/v1/routeFitness.ts:109` | L07/L08 — **unowned, needs a fix** |
| F34 | **A stray `ship-test-pg` container squats 5432 and misleads every lane.** Two lanes independently reported "Postgres is on 5432, not 5433" and both were describing this container, which has been **up 4 days** — it predates this work and no lane started it. CLAUDE.md is correct that `pnpm docker:up` (via `docker-compose.local.yml`) maps **5433:5432**; the compose stack does start there when actually invoked. The problem is that agents find the already-running 5432 first, conclude the docs are wrong, and burn setup time. **Fix is to stop the stray container, not to edit the docs.** (Coordinator note: I twice recorded this as "the lane started its own container" — that was wrong, and it caused me to feed two agents a correction that did not match what they saw.) | `docker ps` → `ship-test-pg`, Up 4 days | unowned |
| F35 | **F5 is worse than recorded.** The internal error shapes do not agree *with each other*: `routes/documents.ts` returns `{error: string}`, `setup.ts` returns `{success, error:{code,message}}`. Left alone deliberately — the UI is written against it — but "keep the internal shape byte-for-byte" means keeping two shapes, not one. | `api/src/routes/documents.ts`, `api/test/setup.ts` | L07 (noted) |


| # | Finding | Evidence | Suggested owner |
|---|---|---|---|
| F1 | Internal rate limiter prefix-mounts over the public API. **Confirmed by execution** against the pinned `express@4.22.1` / `express-rate-limit@8.2.1`: a public route returns `429` with `{"error":"Too many requests. Please slow down."}` and `ratelimit-limit`/`retry-after` headers — internal error shape, wrong header family (no `X-` prefix), and it short-circuits *above* the public router, so no `request_id`, no envelope, and no audit row. | `api/src/app.ts:168` | L08 PF-214 · L11 PF-316/317 |
| F2 | `express.json({limit:'10mb'})` runs app-wide before routers, so the public router's own `json({limit:'1mb'})` is dead code. | `api/src/app.ts:173` | L08 PF-215 |
| F3 | `documents` sorts by mutable `position` with no `(created_at, id)` index. Keyset pagination cannot be reorder-stable on that key — and "cursors stable across reordering operations" is a PRD requirement (p.3). | `api/src/routes/documents.ts:120`, `api/src/db/schema.sql:120` | L08 PF-220/222 |
| F4 | Express pinned at 4.22.1 does not forward async rejections. Without a wrapper the ApiError envelope silently misses the most common failure path. | L07 finding | L07 PF-195 |
| F5 | Internal `/api` has no error middleware at all — 401s are inline `res.status().json()` across route files. Nothing to reuse; the public layer builds its own. | L07 finding | L07 (noted) |
| F6 | `platform/api/v1/errors.ts` sketch comments claim the ApiError code set maps 1:1 onto the SDK `kind` union. It is 6→5: `unauthorized` and `forbidden` both collapse to `kind: 'auth'`. | L07 finding | L07 PF-189 |
| F8 | **There is no `documentService`.** `docs/architecture.md`'s Public/Internal Boundary diagram — the SOLID centerpiece, "both surfaces call the same domain services" — names `utils/document-crud.ts`. That file has no create/update/delete; it only holds association and history helpers. Write SQL is inline in the route files. The shared-service seam has to be extracted before the boundary claim is true. | `api/src/routes/documents.ts:565`, `issues.ts:613` | L14 PF-403 (five tickets depend on it) |
| ~~F9~~ | **PARTLY WRONG AS FILED, and CLOSED by L14.** The claim "nothing sets `properties.status = 'completed'`; the value exists only in a PATCH schema enum" is false — the sprint PATCH handler **does** persist it (`newProps.status = data.status`, then `properties = $n`), and `db:seed` inserts past sprints with `status: 'completed'`. Checked before building on it. What was ACTUALLY missing: (1) no event, because `.publish(` had **zero** call sites repo-wide, not just for this type; (2) no state machine — the PATCH wrote status unguarded, so `active → planning` and `completed → planning` both persisted and stranded the `planned_issue_ids` snapshot a start had taken; (3) two unreconciled notions of status — the server stores `planning\|active\|completed` in `properties` while the frontend DERIVES `active\|upcoming\|completed` from `sprint_number` + workspace start date (`web/src/components/week/WeekTimeline.tsx`), and only the derived one ever produced "completed" in practice. So the fix was not "add a write path" but "make the existing write a guarded transition and hang the event on it" — `api/src/services/sprints.ts`, `SPRINT_TRANSITIONS` as data. Re-completing an already-completed sprint now publishes nothing, which is what makes the event mean "this happened, once". **`sprint.completed` can fire and is tested.** | `api/src/routes/weeks.ts` PATCH handler | L14 PF-407 ✅ |
| F46 | **The api suite was one shared rate-limit bucket away from permanent intermittent failure, and L14's tests pushed it over.** `apiLimiter` (`api/src/app.ts`) is a **module-level** `rateLimit(...)`, so its bucket is shared by every `createApp()` in a worker process and accumulates across test **files** for the whole 60 s window — one budget per run, not per app and not per file. At the old test ceiling of 10 000 the suite (now ~1680 tests) tipped over near the end, and the symptom is a 429 surfacing as an **unrelated assertion failing in a different file on each run** — observed in `auth.test.ts`, `files.test.ts`, `openapi/route.test.ts` and `internal-limiter-scope.test.ts`, each passing in isolation. That shape is the worst possible for CI: a retry masks it and the failure never names the cause. Same family as F33/F44. **Measured, not inferred:** three consecutive full runs each failed one random test at 10 000; two consecutive runs passed 1682/1682 with the ceiling raised. **Fixed by L14** — test-only default raised to 1e6; production (100/min) and dev (1000/min) are byte-for-byte unchanged, and PF-214's `internal-limiter-scope.test.ts` sets `API_RATE_LIMIT_MAX=1` itself so nothing depended on the old default. Three consecutive clean full runs after. **Every lane adding api tests was widening this window**; the real fix is a per-file limiter reset. | `api/src/app.ts` `apiLimiter` | found + fixed by L14 |
| F10 | `DELETE /api/documents/:id` is a hard delete, so an ids-only `document.deleted` payload is unresolvable forever. This alone disproves any universal ids-only payload rule. | `api/src/routes/documents.ts:1153` | L14 PF-409 |
| ~~F11~~ | **CLOSED by L13, and both halves were real.** The spec now mounts through L08's `mountUnauthenticated` seam — inside the router, above `bearerAuth` and above the catch-all. Verified against a really booted server (port 3113, db `ship_l13` on 5433): `curl` with no `Authorization` header returns **200 + `application/json` + `X-Request-Id`**, while `GET /api/v1/documents` on the same server returns 401. **L08's stub is gone**: `router.test.ts` mounted a two-line handler returning `{openapi:'3.1.0',paths:{}}`, so its "every declared unauthenticated path is actually mounted" assertion passed by construction; it now runs against the real handler. ~~Original:~~ would 404 (catch-all) and 401 (`router.use(bearerAuth)`). | L13 finding | L13 PF-365, PF-366 |
| ~~F12~~ | **CLOSED by L13. Verdict: reuse the npm dependency, duplicate nothing else** — separate registry, generator (V31), route, static path (`docs/openapi.json` vs `api/openapi.json`) and tests. The internal registry was **not** reusable: it emits `3.0.0` through `OpenApiGeneratorV3` typed against `oas30`, which fails MVP gate item 7 on the version alone, and it holds ~130 `registerPath()` calls across **90 paths** for `/api/*`. | `api/src/openapi/registry.ts`, `api/src/swagger.ts` | L13 (noted) |
| F41 | **PF-351's acceptance criterion is unsatisfiable as written, and L13 shipped a corrected assertion.** It asks that the public and internal documents share **zero** `paths` keys. Measured: they share `/documents` and `/documents/{id}`, and they do so *because both specs are correct* — a `paths` key is relative to `servers[0]`, and the two servers are `/api` and `/api/v1`. Satisfying it literally would mean renaming the public `documents` resource to win a string comparison. The shipped test compares **fully qualified URLs** (disjoint) and separately proves no internal-only path reaches the public document. | `platform/openapi/registry.test.ts` | L13 (resolved) |
| F42 | **PF-367's premise was wrong: the spec route does NOT bypass the audit sink.** The ticket assumed `bearerAuth → rateLimit → audit`. F7 moved audit **above** bearer auth so 401s and 429s are audited, so the live order is `requestId → audit → body → bodyErrors → unauthenticated → bearerAuth → rateLimit`. Measured: a spec fetch **writes an audit row** (null `clientId`, no token to attribute it to) and consumes **no** rate-limit bucket, proven against a capacity-1 bucket. Only the limiter is bypassed, deliberately — the buckets are keyed `app:`/`token:` and an anonymous request has neither key. Documented as a table in `platform/README.md`. **L11 and L12: the gap is the limiter only, and it is intentional.** | `platform/openapi/route.test.ts` | L11, L12 (informational) |
| F43 | **Ajv cannot validate an OpenAPI 3.1 document, and PF-370 would have shipped a broken validator on the obvious choice.** Ajv 8.17.1 (`ajv/dist/2020` + `ajv-formats`) misresolves the meta-schema's `"schema": {"$dynamicRef": "#meta"}` inside `$defs/parameter` and applies the *Parameter* schema to the parameter's own `schema` value — so `{name:'limit', in:'query', schema:{type:'integer'}}` is rejected for "must have required property 'name'". A validator that wrongly **rejects** is the same class of failure as one that accepts everything. Both packages were installed, measured, and removed; L13 uses `@hyperjump/json-schema@1.17.8`, which implements `$dynamicRef` correctly and **bundles** the OAS meta-schemas (no network at test time). Any lane reaching for a JSON-schema validator should start there. | L13 finding | unowned (informational) |
| F44 | **`documents.pagination.test.ts` PF-259 is planner-dependent and flakes.** "no Seq Scan and no Sort for the FULL public predicate" fails whenever Postgres chooses a sort over the `(created_at DESC, id DESC)` index — which it does on a small/freshly-truncated table. Reproduced **identically on `9af2f01`** (L09's tip, with none of L13's code), so it is pre-existing and not a regression. It passes in most full-suite runs and fails when the file runs alone, which is the worst shape for CI: a retry will mask it. Same species as F33. Fix is to seed enough rows, or to assert the index is *usable* rather than that the planner *used* it. | `api/src/platform/api/v1/documents/documents.pagination.test.ts` | L09 (found by L13) |
| F13 | The webhook bus sketch justifies itself by claiming the FleetGraph agent is a non-HTTP document writer. It isn't — the agent only touches `fleetgraph_*` tables. The real non-HTTP writers are seeds and migrations, and those must **not** publish (`db:seed` inserts 14 documents). | L14 finding | L14 PF-402 |
| F31 | **`terraform/*.tf` and `terraform/environments/*` are two configs of the same resources**, under identical account-global IAM role names. Applying both fails rather than coexisting — they are alternatives, not layers. Pick one root before the first apply. | `terraform/`, `terraform/environments/` | L21 PF-616 |
| F32 | **No Terraform state locking exists anywhere.** No `aws_dynamodb_table`, no `dynamodb_table` in the backend block, no `use_lockfile`. Two concurrent applies would corrupt state — and this lane's whole claim is that the IaC is the source of truth. | `terraform/versions.tf` | L21 PF-621 |
| F36 | **L03's self-deleting assertion cannot fire.** `scope-fitness.test.ts:145` calls `createPublicRouter({...})` with **no `mountResources`** — the hook resource routes arrive through. So `mountedRoutes(router)` stays empty forever and the assertion designed to fail "the day the first resource route lands" never will. It reads as a safeguard and is not one. Fix: pass the real mount, or assert against `createApp()`. L08's `assertEveryRouteDeclaresList` does the equivalent job from the live stack. | `api/src/__tests__/scope-fitness.test.ts:145` | L03 (found by L08) |
| F37 | **Bug in L07's route enumerator, fixed by L08.** `rootStack`'s `??` chain reads `app.router` when `_router` is absent, and Express 4 defines that as a getter that only throws — so `enumerateV1Routes` threw on a route-less app instead of returning `[]`, breaking exactly the vacuity checks that depend on it. | `api/src/platform/api/v1/routeFitness.ts` | L07 (fixed on L08's branch) |
| F38 | **`/api/v1` serves no CORS headers.** Deliberate: the internal `cors` is `origin: <Ship frontend>, credentials: true`, which is wrong for a public API, and moving it broke L01's PF-018 byte-for-byte snapshot. But a browser-based public consumer — including L24's PKCE single-page demo, which is one of the five integrations — needs a policy that does not exist yet. Open item, not a finished decision. | L08 finding | unowned |
| F39 | **Lane branches were a diverging tree, not a stack — coordinator error.** Each lane was told to branch off a different tip, so L01 → {L02, L03, L07} diverged, then L06 branched off L02 and L08 off L07. L07 was never an ancestor of L02, so L06's instruction was impossible as written and it merged L07 in itself to get the seams. Nobody owned integration order. **Resolved 2026-08-12** by building `pf/integration` and merging in dependency order (L02 → L03 → L07 → L08 → L06); four real conflicts, all resolved: `apps/types.ts` (L02 shapes + L03's post-split import), `audit.ts` (same), `scopes/registry.ts` + the pinned `required_scope`→`missing_scope` rename in the live `require-scope.ts`, and the `api/v1/index.ts` barrel. Later lanes must branch off `pf/integration`, not a lane tip. | coordinator | resolved |
| F40 | **L06's report claimed "L08 has committed nothing."** It inspected L08 mid-flight and reported the snapshot as fact — L08 has all six commits including `e71d5e0` (the fresh public router), which L06 concluded was L07's work. Parallel lanes must not draw conclusions about each other's completeness from a point-in-time read. | L06 report vs `git log` | noted |
| F41 | **Every E2E worker ran against a database with NO OAuth tables — and nothing said so.** `e2e/fixtures/isolated-env.ts` marks every migration as applied in `schema_migrations`, then actually executes only a hard-coded list containing `['038_fleetgraph.sql']`, while `schema.sql` stops at 037. **Migrations 039–066 never ran on any worker.** The `schema_migrations` INSERT is what made it silent: the DB claimed to be current. Fixed by L04 as a *rule* — everything above a stated cut-off, applied in order, loud on failure — rather than by appending filenames, since appending is exactly what produced it. **L02 and L06 must re-check their E2E coverage; it was vacuous or absent.** | `e2e/fixtures/isolated-env.ts` | fixed by L04 |
| F42 | **`enumerateV1Routes` throws on any app with a RegExp route**, and `createApp` mounts one whenever `web/dist` exists — so the harness passes before `pnpm build:web` and throws after. Production boot is unaffected; CI ordering decides whether anyone sees it. L09 hit the same class and coerced its case. | `platform/api/v1/routeFitness.ts` | L07/L08 |
| F43 | **Cross-workspace token minting, found and closed by L04 — was in no ticket.** `issueTokenPair` stamps the token with `app.workspaceId`, so a user in workspace A consenting to an app registered in B would mint a **B-scoped token on an A session**. `client_id`s are not secret, so this was reachable by anyone who could read one. Now 403, no row written. | L04 finding | closed |
| F44 | **`documents.pagination.test.ts` PF-259 fails standalone, passes in most full-suite runs.** It asserts the planner uses the keyset index; on a small table Postgres sorts instead. **Reproduced identically on `9af2f01` with none of L13's code** — pre-existing, not caused by the lane that found it. That pass-in-suite/fail-alone shape is exactly what a CI retry masks. | `api/src/__tests__/documents.pagination.test.ts` | L09/L08 |
| F45 | **PF-367's premise was false and L11/L12 need to know.** It assumed the order `bearerAuth → rateLimit → audit`; F7 moved audit **above** bearer auth, so the spec route **does** write an audit row (with a null `clientId`). Only the rate limiter is bypassed. | L13 finding | L11, L12 |
| F46 | **A test rewrites a graded submission artifact.** `api/src/platform/openapi/staticCopy.test.ts` calls `writePublicSpec()` for real, so `pnpm test` **overwrites the committed `docs/openapi.json`** — and a route module absent from *that test file's* import list is silently deleted from the artifact. L10 watched `/me` generate correctly and vanish minutes later. It extended the import list as a stopgap; the durable fix is writing to a temp path and comparing, which is L13's call. PRD p.13 requires that static copy as a submission deliverable. | `api/src/platform/openapi/staticCopy.test.ts` | L13 |
| F48 | **Three stale webhook sketch types in `platform/apps/types.ts` are a BUILD failure waiting for whoever declares the real ones.** `WebhookSubscription`, `WebhookDelivery` and `DeliveryStatus` sit in L02's types file with **zero consumers repo-wide** (verified by grep). Both `apps/` and `webhooks/` are re-exported through `platform/index.ts`, so the moment a lane declares the real type the duplicate name is **TS2308**, not a style problem — `pnpm type-check` fails and the build stops. L15 hit this on `WebhookSubscription` and removed that one declaration, leaving a comment in its place; the sketch was wrong in every field besides (`signingSecretHash` — PF-422 encrypts, it cannot hash; `eventTypes: string[]` — p.7's drill loop is a singular `event`; camelCase where the public representation is snake_case). **`WebhookDelivery` and `DeliveryStatus` are still there and will do the same thing to L16** the day it declares a delivery row. Left alone deliberately: they are L16's to name, and deleting another lane's types twice is worse than telling them once. | `api/src/platform/apps/types.ts`; TS2308 on `platform/index.ts` | L16 (the two remaining), L02 (informational) |
| F49 | **`api/src/test/setup.ts`'s TRUNCATE list is a hand-maintained list of table names, and that is the mechanism behind F33/F46-class flake.** A table absent from it keeps rows across test *files*, and the symptom is an unrelated assertion failing in a different file on each run — `public_api_calls` did exactly this to L12 for days. L15 added `webhook_subscriptions` in the same commit as migration 047, but the general defect stands: the list is a second place to remember, and the person adding migration NNN is not reading `setup.ts`. Same species as F41, which L04 fixed by replacing a hand-maintained migration list with a **rule**. The equivalent here is to derive the list from `information_schema.tables` minus an explicit keep-list, so adding a table is automatically covered and *excluding* one is the deliberate act. Not fixed by L15 — it is one line for this lane and a shared-fixture change for the repo. | `api/src/test/setup.ts` | unowned — needs an owner |
| F47 | **`pf/integration` does not build from a clean checkout.** 30 files fail with `Failed to resolve entry for package "@ship/shared"` / `"@ship/agent"` until `pnpm build:shared`, `pnpm --filter @ship/agent build` and `pnpm --filter @ship/sdk build` have run. Every lane has hit this independently. MVP gate item 9 is "regression suite passes" and CI will hit it too. | build order | unowned |
| F29 | **`/oauth/*` has no rate limit at all.** L11 is scoped to `/api/v1`, and L04's PF-107 asserts the internal `apiLimiter` does not reach the OAuth router — so `/oauth/token`, a credential-presenting endpoint, is unthrottled against brute force. L05's PF-132 built a throttle for its own `user_code` guess space only. Generalizing it needs L11's agreement; no lane currently owns it. | L04 PF-107, L05 PF-132 | **unowned — needs an owner** |
| F30 | No token-revocation endpoint and no "your devices" UI. RFC 7009 appears nowhere in p.10's stack list. L06's PF-165 gives the repo the capability that L02's leaked-secret playbook (PF-049) depends on, but nothing exposes it. | — | L06 PF-165 (capability only) |
| F27 | **`playwright.config.ts:60` is `retries: process.env.CI ? 2 : 1`.** A TTFE drill written into the Playwright suite silently forfeits the **0% flake over 20 runs** target (p.9) — a retry is precisely what converts a flake into a pass. L20 runs the drill under vitest with `retry: 0`, which also keeps it out of the 150-minute `e2e` job and clear of the standing rule against `pnpm test:e2e`. | `playwright.config.ts:60` | L20 |
| F28 | Three CI constraints in `.gitlab-ci.yml`, each of which has already broken a job: `workflow.rules` sets `merge_request_event → never`, so "runs on every PR" must be served by the **branch** pipeline and an MR-only rule never fires · testcontainers use the runner's **mounted** Docker socket (`TESTCONTAINERS_HOST_OVERRIDE`, Ryuk disabled), so a `docker:dind` service demonstrably never attaches · jobs need `scripts/assert-tests-ran.sh 1 --` or a zero-stage run reads as a pass. | `.gitlab-ci.yml` | L20 |
| F25 | **`web/src/lib/queryClient.ts` persists the React Query cache to IndexedDB, and it survives reload and logout.** A `client_secret` or webhook signing secret that passes through query state — both are shown-once values the portal must display — lands on disk. p.15's shown-once leakage question asks about screenshots, logs and the back button; this is a fourth channel none of them cover. Measured, not inferred. | `web/src/lib/queryClient.ts` | L22 PF-667 (asserts against the store, not a code review) |
| F26 | `api/src/app.ts:73` skips CSRF on any `Authorization: Bearer` header. Safe **today** only because `api/src/middleware/auth.ts:135` does not fall back to session auth on an invalid bearer — so a forged bearer gets 401 rather than riding the session. Nothing pins that coupling; if the fallback is ever added, the CSRF skip becomes a bypass. | `api/src/app.ts:73`, `middleware/auth.ts:135` | L22 PF-665 (regression test) |
| ~~F19~~ | **FIXED by L17 (PF-491).** `baseUrl` is optional with a documented resolution order (explicit option → `SHIP_BASE_URL` → published instance). The gate's literal expression `new ShipClient({ token })` is now compiled by `pnpm type-check` as a permanent fixture — `sdk/typeProofs/gateItem8.ts` — so it cannot silently become a type error again. ~~Original:~~ `ShipClientOptions.baseUrl` is required, so the MVP gate item 8 expression fails compilation. | `sdk/src/client.ts` | L17 PF-491 ✅ |
| ~~F20~~ | **FIXED by L17 (PF-494).** `buildRequestUrl` preserves the base URL's mount path; `sdk/src/baseUrl.test.ts` tables bare origin, trailing slash, one-segment prefix and multi-segment prefix against four paths, and pins the old behaviour as the documented regression. Proven live in `sdkGate.test.ts` §3 against a server mounted under `/ship`. ~~Original:~~ `new URL('/api/v1' + path, baseUrl)` discards any path prefix, so every call 404s behind one. | `sdk/src/baseUrl.ts` | L17 PF-494 ✅ |
| F27 | **A public client cannot refresh.** `/oauth/token`'s `authenticateClient` returns `null` unless BOTH `client_id` and `client_secret` are presented (Basic or body), so the refresh grant is unreachable for a public client — which is what a CLI is, per RFC 6749 §2.1, and why L04 built PKCE for it in the first place. The auth-code+PKCE flow therefore hands L19's `ship login` a refresh token it can never spend: the first expiry is a full re-login, which fails the p.8 TTFE drill on the second command exactly the way D8's access-only store would have. Found by L17 while wiring PF-509's refresh — the SDK sends the secret when it has one and reports an actionable `{kind:'auth'}` when it does not, but it cannot invent a credential. Fix is server-side: accept `client_id` alone for a client registered as public. | `api/src/platform/oauth/router.ts` (`authenticateClient`) | L06 (fix) + L19 (consumer) |
| F21 | SDK `paginate` loops `while (cursor !== null)`. A response that **omits** `next_cursor` yields `undefined`, which `!== null` — so the SDK re-requests page 1 forever. | `sdk/src/pagination.ts` | L18 PF-535 |
| F22 | `verifyWebhook`'s header lookup misses a WHATWG `Headers` object and returns `false` on a **valid** signature. Silent false-negative; every `fetch`-based subscriber hits it. | `sdk/src/webhooks.ts` | L18 PF-546 |
| F23 | The `verifyWebhook` sketch takes an options object as its fourth parameter; p.7, `docs/architecture.md`, and L15's PF-438 all specify positional `toleranceSec = 300`. Three graded sources to one. | `sdk/src/webhooks.ts` | L18 PF-542 |
| ~~F24~~ | **FIXED — both halves by L01, VERIFIED by L17 (PF-515).** ESLint fence 4 (`sdk/**` may import nothing from this repo) and its negative fixture ship in `eslint.config.js` / `eslint-fixtures/sdk/`; `pnpm lint:boundary` reports it green as "F24 sdk → workspace" against L17's new code. The CI step existed with `--passWithNoTests` because "the suites arrive with L17/L18" — they have (161 tests), so the flag is now belt-and-braces rather than the reason the step passes. L17 added the leg the finding did not ask for: a **blocking** `size:check` step, because a test step proves the code runs and nothing proved the p.9 budget. | `eslint.config.js`, `.github/workflows/ci.yml` | L01 + L17 PF-515 ✅ |
| F15 | **`documents.created_at` is nullable.** A row-comparison keyset `(created_at, id) < ($1,$2)` evaluates to NULL for such a row, so it is **invisible on every page** — not misordered, absent. Silent data loss through the flagship public endpoint. L08's PF-222 ships the index; nothing ships the NOT NULL constraint. | `api/src/db/schema.sql:153` | L09 PF-259 |
| F16 | **Scope leak.** `/api/v1/documents` unfiltered serves issues, sprints, programs, projects and people under `documents:read`, and `POST` accepts `document_type:'issue'`. The unified document model means one table — but one *scope* over all of it is not what p.3 registers. Fix is `PUBLIC_DOCUMENT_TYPES` as data; consequence is that several document types become publicly unreachable this week. | `api/src/routes/documents.ts:36`, `:146` | L09 PF-250 |
| F17 | Internal create returns `RETURNING *` — `yjs_state`, `deleted_at`, `position` would all ship to external consumers. The public projection has to be an allowlist, not an exclusion list. | `api/src/routes/documents.ts:565` | L09 PF-252 |
| F18 | Both remaining internal sort keys are unusable for stable keyset pagination: issues sort by mutable priority + `updated_at`; sprints by `(properties->>'sprint_number')::int`, an unindexed JSONB expression. | `api/src/routes/issues.ts:212`, `weeks.ts:353` | L10 PF-281/288 |
| ~~F14~~ | **FIXED by L17 (PF-507).** Conditional `exports` map: the `browser` condition resolves to a new `sdk/src/browser.ts` whose transitive import graph contains **no `node:` specifier and no bare import at all** — asserted by a graph walk in `sdk/src/fitness.test.ts`, not by inspection — while every other condition still resolves to `index.ts`, so no Node consumer of `verifyWebhook` breaks. `@ship/sdk/browser` and `@ship/sdk/node` exist for explicit callers. **L24's PF-738 detector is unblocked and should now pass.** ~~Original:~~ the barrel re-exported `verifyWebhook`, whose module top-level-imports `node:crypto`, so any browser bundle failed to resolve or polyfilled crypto against the < 250 KB budget (p.9). Found independently by L17 and L24. | `sdk/src/browser.ts`, `sdk/package.json` | L17 PF-507 ✅ → L24 PF-738 (detector, unblocked) |
| ~~F7~~ | **RESOLVED by L07, and it was real.** Audit middleware sat below bearer auth and rate limiting, so 401s and 429s registered no `res.on('finish')` hook and were **never audited** — silently exempting exactly the traffic an audit trail exists for (p.4). PF-193's assertion caught it. Audit moved above auth; nothing already recorded changes. **L12 must know the documented order moved.** ~~Original:~~ Middleware order conflict. The `router.ts` sketch mounts `bearerAuth → rateLimit → audit`, but `publicAuditMiddleware` hooks `res.on('finish')` — under that order 401s and 429s are never audited, contradicting L07 PF-193. Consequence once fixed: audit rows for unauthenticated traffic carry null `clientId`/`userId`. | L08 PF-213 `⚑` | L08 + L12 |

## Documentation drift — `docs/architecture.md` vs. reality

| # | Finding | Raised by |
|---|---|---|
| G1 | Claims the agent's OAuth app is seeded **by migration**. The repo seeds via `seed.ts`. The PRD only asks the question (p.17); the commitment is ours and is new work, not an existing pattern. | L02 |
| G2 | The audit-field list (line ~18) omits `request_id`, which the PRD names (p.18) and which `ApiError` already carries (line 56). | L25 |
| G3 | Claims the Terraform delta is "env-group entries only." There is no `render_env_group` resource — env vars are an inline map on the web service. The zero-new-resources claim holds; the sourcing didn't. | L21 |
| G4 | Never names the agent's OAuth grant type, though PRD p.17 demands the choice be defended and p.13 puts it in interview prep. See D5. | L25 |

## PRD internal contradictions — both halves verified

| # | Contradiction | Pages |
|---|---|---|
| C1 | Final Submission deadline: **11:59 AM** in the Project Overview table vs **11:59 PM** in Submission Requirements. | p.1 vs p.12 |
| C2 | Deployment target: Technical Stack explicitly permits Render; MVP gate and the Terraform table demand IAM task role + execution role, VPC/subnets, security groups — AWS/ECS language. What is deployed is `terraform/render/` with zero IAM/VPC/SG. | p.10 vs p.2, p.5 |
| C3 | Webhook signing secret is "**hashed**" at rest (p.3), but Failure Modes presumes the server re-signs each attempt with the subscription's *current* secret (p.12). HMAC-SHA256 is symmetric — a one-way hash cannot key it. **Not a wording quibble; the two requirements are mutually impossible.** Resolved by L15 PF-422 as AES-256-GCM encrypted at rest, key in env, `secret_prefix` in clear for identification, fail-closed decryption. The tempting non-answer — store `sha256(secret)` and sign with *that* — satisfies the word and is theater: whatever the server signs with **is** the key, so a DB compromise forges signatures either way, and it silently breaks the published `verifyWebhook(headers, rawBody, secret)` signature on p.7. Note the asymmetry with `client_secret`, which correctly stays hashed: a client secret is *presented back to us* and verified by comparison; a webhook secret is *used by us to produce a MAC* and is never presented. | p.3 vs p.12 |

## Requirements no lane could cleanly ticket

| # | Requirement | Why | Raised by |
|---|---|---|---|
| ~~U1~~ | ~~"Verify an action outside the policy is DENIED" (p.5) has no credential-scoping surface on Render.~~ **CLOSED by D6.** On AWS the surface exists: L21's PF-637 records an `AccessDenied` from the EB instance profile alongside an in-policy call succeeding in the same transcript. No Postgres-role substitution, nothing to caveat. | L21 |
| U2 | `/api/v1/scopes` discovery endpoint appears only as an example *inside* a pagination question (p.16), not as a requirement. Deliberately left out. | L03 |
| U3 | The PRD's only `400` (`invalid_grant` on `/oauth/token`, p.2) is RFC 6749's format on a non-`/api/v1` route and has no slot in the ApiError union. If L04/L05/L06 route OAuth errors through the public envelope, that is a contract violation. | L07 |
| U4 | Page size, pagination param name, and sort direction are unspecified. L08 chose `limit`, 25/100, newest-first and marked them `—` rather than inventing a citation. | L08 |
| U6 | **No lane gives an externally-hosted webhook listener a public URL, and two lanes need one.** L24's Slack integration (PF-741–744) runs against localhost until someone hosts the listener; L19's `ship webhooks tail` needs deliveries to reach a developer's laptop, which the PRD never solves (local listener + tunnel, a relay, or long-poll against the delivery log). Same root cause, two lanes, and it is the largest execution risk in both. L21 owns hosting, L26 owns the deployed-application submission row — one of them has to claim it. | L24, L19 |
| ~~U5~~ | ~~The 2s webhook delivery P95 (p.6) is proven by no lane.~~ **CLAIMED by L20 PF-603**, with the honest caveat that one drill run is one sample and needs the 20-run series to be a P95 at all. | L15 → L20 |

## Cross-lane boundary disputes — two lanes disagree, neither is wrong

| # | Dispute | Detail |
|---|---|---|
| B1 | **L15/L16 seam.** L15 unilaterally placed `IWebhookDeliverer` plus the bus→matcher→signer hand-off in its own lane so the signer is testable, leaving L16 the HTTP concrete, retries, log, DLQ and replay. L16 may reasonably claim the interface. Settle before either builds. | L15 |
| B2 | **L14 `publish()` on the request path.** L14's PF-399 awaits every handler and PF-404 puts that on the request path — so a handler doing network I/O drags a third party inside MVP-9's +10% P95 budget. L15's PF-441 signs and enqueues *without* awaiting the wire, reading p.11's "in-memory deliverer resolves synchronously" as scoped to tests. If L16 reads it as production too, the three lanes disagree. **L14 does not know L15 constrained its contract.** | L15 |
| B5 | **Same extraction ticketed twice.** L09's PF-241 and L14's PF-403 both cut the domain-service seam out of the route handlers. L09 is tier 3 and must land first — a public route with no service to call inlines SQL, and the boundary claim is false from commit one. **Recommended split (both lanes' notes agree):** PF-241 owns `list`/`get`/`create`; PF-403 shrinks to "add the publish inside the already-extracted service, plus `update`/`delete`." Resolve before the build fans out or two agents write the same file. | L09, L14 |
| ~~B6~~ | **RESOLVED by L03.** Distinguished structurally, not by convention: `scope: null` is an explicit claim and passes; a property present but `undefined` throws at wiring time; **no record at all** is the real forgot-case and is invisible to anything that only reads the declaration table. So `auditRouterScopes()` walks the Express router stack for ground truth and consults the table for what each route claimed — reading the table alone is structurally incapable of catching a route that never declared. Violations report as `kind: 'undeclared'` vs `'unregistered'`, which is what stops someone reaching for `scope: null` to quiet CI. L10's PF-271 can now declare null for `/me` with PF-062's exactly-seven assertion untouched. ~~Original:~~ `/api/v1/me` has no scope, and L03 forbids that. p.3 registers exactly seven scopes; L03's PF-062 asserts exactly seven and PF-079 fails any route without a *registered* scope. L10's fix is `scope: null` as an explicit declared value — which **requires amending L03's PF-079**. Flagged rather than changed unilaterally. | L10 PF-271 `⚑` |
| ~~B7~~ | **RESOLVED by L03 — with a correction: it was never a move.** The file did not exist; S4 is the slice that creates it, so this was a first write to `platform/api/v1/resource-map.ts`, not a `git mv`. L10's reasoning was right about the destination. ~~Original:~~ Sprints/weeks map is in the wrong directory, and now two lanes say so. L03 placed `resource-map.ts` under `platform/scopes/` and doubted it; L10 independently concluded it belongs in `platform/api/v1/`. The map's key is a URL path segment and its value is a domain module — it shares only a substring with the scope name `sprints:read`. Nothing in `scopes/` reads it: `requireScope` takes a scope name and never resolves a resource. L10 and L14's PF-396 both do. One-commit move; assertions hold under either path. | L03, L10 |
| B12 | **A read-only agent (D5b) changes Part 2's agent behavior, and Part 2's tests must still pass with the flag both on and off (p.17).** Measured: the agent's three action types are `comment`, `history_note` and `notify` (`agent/src/actions/act.ts:74,77,83`). The first two reach Ship through `POST /api/issues/:id/history` and `PATCH /api/issues/:id`; **the public API exposes neither, and the seven scopes on p.3 cover neither.** So under D5b the agent's two write actions have to become recommendations surfaced through `fleetgraph_notifications` — its own table, never the public API. That is what makes Epic 7's claim literally true (every action it takes *is* a public API call, so the audit log has no holes), and it is also a real change to what the agent does. **ANSWERED by L23: the suite does NOT pass byte-for-byte in both states.** `e2e/fleetgraph-agent.spec.ts:431–443` asserts a comment containing `— FleetGraph`; flag-on there is no comment, so it is false by design. PF-707 forks that one assertion — flag-off asserts the comment, flag-on asserts a `kind='recommendation'` row carrying the same measurement — so both halves assert the same property: the finding reached a human. The write-up must say "passes in both states at the suite level, with one assertion forked and named." **Correction to the earlier entry:** the comment action goes through `POST /api/documents/:id/comments` (`agent/src/actions/client.ts:147`), not `PATCH /api/issues/:id` — that appears only in a refusal string that never issues a request. The public API is therefore missing a **comments** route as well as history. `docs/architecture.md:178` says "the scopes its detectors and actions need" without naming them — that sentence needs reconciling to read-only. | L23, L26 |
| ~~B14~~ | **DECIDED 2026-08-12: option (a) — an optional `details.reason` enum on `unauthorized` only.** L07 found the two tickets literally contradict: PF-198 says `unauthorized` MUST omit `details`, PF-161 puts the expired-token distinction *in* `details.reason`, and the schema rejects a 401 carrying details today. L07 implemented PF-198 as written rather than silently widening it — correct call. Rejected: a seventh `ApiErrorCode`, which contradicts the six-member union printed verbatim on PRD p.7 and would be a three-lane change (L06 + L07 + L17's key-equality assertion); and `WWW-Authenticate`, which is RFC-correct but the gate says "error code" and a header is not one. **L07 widens the policy by exactly one optional member; L06's PF-161 then works as ticketed.** ~~Original:~~ MVP gate item 3 (p.2) requires expired tokens to return "401 with a distinct error code." L07's `ApiErrorCode` union is closed at six and printed verbatim on p.7, and L17's PF-498 asserts key-equality against it. L06's PF-161 therefore puts the distinction in `details.reason` rather than adding a seventh code. If you read the gate clause as demanding a distinct top-level `code`, that is a three-lane change (L06 + L07 + L17) and contradicts a printed interface. | L06 PF-161 `⚑` |
| B13 | **`docs/architecture.md:178` is edited by three lanes — same paragraph, three owners.** L26's PF-788 (seeding), L26's PF-791 (grant type), and L23's PF-712 (scopes + the recommendation change). Sequence them or land all three in one PR; otherwise the last writer silently reverts the other two in a graded document. | L23, L26 |
| B8 | **The demo cannot be recorded under L15's own validation.** L15's PF-425 permits `http://localhost` target URLs only when `NODE_ENV === 'test'`. Drill passes, CI passes, and the person recording the demo video gets `validation_failed`. L19 PF-575 asks for one named default-off opt-in flag rather than weakening the check. | L19, L15 |
| B9 | **`--poll` cannot honestly claim "signature verified ✓".** L16 stores the signed `rawBody` but not the `Ship-Signature` header actually sent, so the long-poll path has nothing to verify against. L19 PF-576 accepts either one nullable column from L16, or the mode printing "not verifiable in poll mode" — silently omitting the line is excluded. | L19, L16 |
| B10 | **The audit view has no route.** L12's PF-343 ships `listCalls(...)`, a repository function React cannot call, and p.4 gives Replay a path while giving the audit trail none. L22's PF-675 leans owner-scoped internal route but flags it as genuinely cross-lane. | L22, L12 |
| B11 | **Portal traffic is indistinguishable from the developer's own in the audit trail.** L12's `PublicApiCallRecord` is a closed key set asserted against a literal array, so adding a "from the portal" field is a cross-lane edit to the very ticket that exists to prevent it. L22's PF-676 discloses the limitation in the UI instead. | L22, L12 |
| B4 | **The DLQ is not the runaway-cost ceiling.** Pre-Search 1.2 (p.15) asks what bounds a subscriber that 5xx's forever multiplied by every event. The DLQ caps attempts per *delivery*, not deliveries per *subscription* — so a permanently-broken subscriber still costs 6 attempts per event, forever. The actual ceiling is the per-subscription circuit breaker (L16 PF-482, reusing `@ship/shared`'s existing `CircuitBreaker` via L01's `Clock`). Anyone reading "DLQ after 6 attempts" as the cost answer is wrong. | L16 |
| B3 | **Migration ordering.** L15's PF-421 declares an FK to `oauth_apps`, a table owned by L02 — which is unwritten and blocked on D1–D3. Ordering against L01's reserved migration block (PF-021) needs settling between the lanes. | L15 |
